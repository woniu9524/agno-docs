---
title: SentenceTransformers Embedder
sidebarTitle: SentenceTransformers
---

`SentenceTransformerEmbedder` クラスは、[SentenceTransformers](https://www.sbert.net/) ライブラリを使用してテキストデータをベクトルに埋め込むために使用されます。

## 使用方法

```python cookbook/embedders/sentence_transformer_embedder.py
from agno.agent import AgentKnowledge
from agno.vectordb.pgvector import PgVector
from agno.embedder.sentence_transformer import SentenceTransformerEmbedder

# Embed sentence in database
embeddings = SentenceTransformerEmbedder().get_embedding("The quick brown fox jumps over the lazy dog.")

# Print the embeddings and their dimensions
print(f"Embeddings: {embeddings[:5]}")
print(f"Dimensions: {len(embeddings)}")

# Use an embedder in a knowledge base
knowledge_base = AgentKnowledge(
    vector_db=PgVector(
        db_url="postgresql+psycopg://ai:ai@localhost:5532/ai",
        table_name="sentence_transformer_embeddings",
        embedder=SentenceTransformerEmbedder(),
    ),
    num_documents=2,
)
```

## パラメータ

| パラメータ                     | タイプ               | デフォルト             | 説明                                       |
| ----------------------------- | ------------------ | ------------------- | ------------------------------------------ |
| `dimensions`                  | `int`              | -                   | 生成される埋め込みの次元数                 |
| `model`                       | `str`              | `all-mpnet-base-v2` | 使用する SentenceTransformers モデルの名前 |
| `sentence_transformer_client` | `Optional[Client]` | -                   | 事前に設定された SentenceTransformers クライアントインスタンス（オプション） |

## 開発者向けリソース
- [Cookbook](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/embedders/sentence_transformer_embedder.py) を表示