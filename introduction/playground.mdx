---
title: Playground
description: "**Agno 提供了一个直观的界面，用于测试和交互您的 AI 代理。**"
---


<Frame caption="Agno 平台 - Playground">
  <img
    height="200"
    src="/images/playground.png"
    style={{ borderRadius: '8px' }}
  />
</Frame>


Playground 提供了一个强大的界面，具有丰富的功能，可用于测试您的代理系统。

- **流式支持**: 实时响应流式传输和中间状态回传给用户。

- **会话历史**: 在 Playground 中直接可视化对话历史。

- **用户记忆**: 可视化跨对话的用户详细信息和偏好。

- **配置**: 全面的配置界面，允许您查看代理参数、模型设置和工具配置。

- **推理支持**: 内置支持，可在 Playground 界面中显示详细的推理跟踪。

- **人工干预支持**: 通过专门的人工监督和批准，在代理工作流中启用手动干预。

- **多模态支持**: 支持处理和生成文本、图像、音频和其他媒体类型。

- **多代理系统**: 支持多代理团队和工作流。

## 在本地与您的代理交互

<Steps>

<Step title="创建包含示例代码的文件">



```python playground.py
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.playground import Playground
from agno.storage.sqlite import SqliteStorage
from agno.tools.duckduckgo import DuckDuckGoTools
from agno.tools.yfinance import YFinanceTools

agent_storage: str = "tmp/agents.db"

web_agent = Agent(
    name="Web Agent",
    model=OpenAIChat(id="gpt-4o"),
    tools=[DuckDuckGoTools()],
    instructions=["Always include sources"],
    # Store the agent sessions in a sqlite database
    storage=SqliteStorage(table_name="web_agent", db_file=agent_storage),
    # Adds the current date and time to the instructions
    add_datetime_to_instructions=True,
    # Adds the history of the conversation to the messages
    add_history_to_messages=True,
    # Number of history responses to add to the messages
    num_history_responses=5,
    # Adds markdown formatting to the messages
    markdown=True,
)

finance_agent = Agent(
    name="Finance Agent",
    model=OpenAIChat(id="gpt-4o"),
    tools=[YFinanceTools(stock_price=True, analyst_recommendations=True, company_info=True, company_news=True)],
    instructions=["Always use tables to display data"],
    storage=SqliteStorage(table_name="finance_agent", db_file=agent_storage),
    add_datetime_to_instructions=True,
    add_history_to_messages=True,
    num_history_responses=5,
    markdown=True,
)

playground_app = Playground(agents=[web_agent, finance_agent])
app = playground_app.get_app()

if __name__ == "__main__":
    playground_app.serve("playground:app", reload=True)
```

在运行 Playground 应用程序之前，请记住导出您的 `OPENAI_API_KEY`。

<Tip>确保 `serve()` 指向包含您的 `Playground` 应用程序的文件。</Tip>

</Step>

<Step title="使用 Agno 进行身份验证">

使用 [agno.com](https://app.agno.com) 进行身份验证，以便您的本地应用程序可以告知 Agno 您正在运行 Playground 的端口。

请查看 [身份验证指南](how-to/authentication) 以获取有关如何使用 Agno 进行身份验证的说明。

<Note>
不会将任何数据发送到 agno.com，所有代理数据都存储在您的 sqlite 数据库中。
</Note>



</Step>

<Step title="运行 Playground 服务器">

安装依赖项并运行您的 Playground 服务器：

```shell
pip install openai duckduckgo-search yfinance sqlalchemy 'fastapi[standard]' agno

python playground.py
```

</Step>

<Step title="查看 Playground">

- 打开提供的链接或导航到 `http://app.agno.com/playground`（需要登录）。
- 添加/选择 `localhost:7777` 端点，然后开始与您的代理聊天！

<video
  autoPlay
  muted
  controls
  className="w-full aspect-video"
  src="/videos/playground.mp4"
></video>

</Step>

</Steps>
<Accordion title="寻找自托管的替代方案？">

寻找自托管的替代方案？查看我们的 [开源代理 UI](https://github.com/agno-agi/agent-ui) - 一个使用 Next.js 和 TypeScript 构建的现代代理界面，其工作方式与 Agent Playground 完全相同。

<img
  src="/images/agent-ui.png"
  style={{ borderRadius: '10px', width: '100%', maxWidth: '800px' }}
  alt="agent-ui"
/>

### 开始使用 Agent UI

```bash
# 创建一个新的 Agent UI 项目
npx create-agent-ui@latest

# 或者手动克隆并运行
git clone https://github.com/agno-agi/agent-ui.git
cd agent-ui && pnpm install && pnpm dev
```

UI 将默认连接到 `localhost:7777`，与上面的 Playground 设置匹配。有关更多详细信息，请访问 [GitHub](https://github.com/agno-agi/agent-ui)。

</Accordion>

<Info>遇到连接问题？查看我们的 [故障排除指南](/faq/playground-connection)</Info>