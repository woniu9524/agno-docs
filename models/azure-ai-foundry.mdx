---
title: Azure AI Foundry
description: Learn how to use Azure AI Foundry models in Agno.
---

使用托管在 Azure 基础架构上的各种开源模型。在此处了解更多信息：[https://learn.microsoft.com/azure/ai-services/models](https://learn.microsoft.com/azure/ai-services/models)。

Azure AI Foundry 提供对 `Phi`、`Llama`、`Mistral`、`Cohere` 等模型的访问。

## 身份验证

导航到 [Azure Portal](https://portal.azure.com/) 中的 Azure AI Foundry 并创建一个服务。然后设置您的环境变量：

<CodeGroup>

```bash Mac
export AZURE_API_KEY=***
export AZURE_ENDPOINT=***  # 格式为 https://<your-host-name>.<your-azure-region>.models.ai.azure.com/models
# 可选：
# export AZURE_API_VERSION=***
```

```bash Windows
setx AZURE_API_KEY ***  # 格式为 https://<your-host-name>.<your-azure-region>.models.ai.azure.com/models
setx AZURE_ENDPOINT ***
# 可选：
# setx AZURE_API_VERSION ***
```

</CodeGroup>

## 示例

将 `AzureAIFoundry` 与您的 `Agent` 一起使用：

<CodeGroup>

```python agent.py
from agno.agent import Agent
from agno.models.azure import AzureAIFoundry

agent = Agent(
    model=AzureAIFoundry(id="Phi-4"),
    markdown=True
)

# 在终端上打印响应
agent.print_response("Share a 2 sentence horror story.")
```

</CodeGroup>

## 高级示例

在此处查看更多示例：[../examples/models/azure/ai_foundry](../examples/models/azure/ai_foundry)。

## 参数

<Snippet file="model-azure-ai-foundry-params.mdx" />

`AzureAIFoundry` 是 [Model](/reference/models/model) 类的子类，可以访问相同的参数。