---
title: OpenAI Like
description: Agno で OpenAI ライクなモデルを使用する方法を学びます。
---

Together、Groq、Nebius、Sambanova などの多くのプロバイダーは、OpenAI API 形式をサポートしています。`OpenAILike` モデルを使用して、`base_url` を置き換えることでアクセスできます。

## 例

<CodeGroup>

```python agent.py
from os import getenv
from agno.agent import Agent, RunResponse
from agno.models.openai.like import OpenAILike

agent = Agent(
    model=OpenAILike(
        id="mistralai/Mixtral-8x7B-Instruct-v0.1",
        api_key=getenv("TOGETHER_API_KEY"),
        base_url="https://api.together.xyz/v1",
    )
)

# ターミナルに応答を表示します
agent.print_response("Share a 2 sentence horror story.")
```

</CodeGroup>

## パラメータ

<Snippet file="model-openai-like-reference.mdx" />

`OpenAILike` は、[OpenAIChat](/reference/models/openai) のすべてのパラメータもサポートしています。