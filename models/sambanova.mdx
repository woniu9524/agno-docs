---
title: Sambanova
description: 了解如何将 Sambanova 与 Agno 结合使用。
---

Sambanova 是一个为大型语言模型提供端点的平台。请注意，Sambanova 目前不支持函数调用。

## 身份验证

设置您的 `SAMBANOVA_API_KEY` 环境变量。在此处获取您的密钥：[https://cloud.sambanova.ai/apis](https://cloud.sambanova.ai/apis)。

<CodeGroup>

```bash Mac
export SAMBANOVA_API_KEY=***
```

```bash Windows
setx SAMBANOVA_API_KEY ***
```

</CodeGroup>

## 示例

将 `Sambanova` 与您的 `Agent` 一起使用：

<CodeGroup>

```python agent.py
from agno.agent import Agent, RunResponse
from agno.models.sambanova import Sambanova

agent = Agent(model=Sambanova(), markdown=True)

# 在终端中打印响应
agent.print_response("写一个两句话的恐怖故事。")

```

</CodeGroup>

## 参数

<Snippet file="model-sambanova-params.mdx" />

`Sambanova` 还支持 [OpenAI](/reference/models/openai) 的参数。