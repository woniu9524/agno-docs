---
title: Agentic Chunking
---

Agentic chunking は、モデルを使用してテキスト内の自然な区切りを判断することにより、ドキュメントをより小さなチャンクに分割するインテリジェントな方法です。固定された文字数でテキストを分割するのではなく、コンテンツを分析して、段落の区切りやトピックの遷移のような意味的に意味のある境界を見つけます。

## Usage

```python
from agno.agent import Agent
from agno.document.chunking.agentic import AgenticChunking
from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
from agno.vectordb.pgvector import PgVector

db_url = "postgresql+psycopg://ai:ai@localhost:5532/ai"

knowledge_base = PDFUrlKnowledgeBase(
    urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
    vector_db=PgVector(table_name="recipes_agentic_chunking", db_url=db_url),
    chunking_strategy=AgenticChunking(),
)
knowledge_base.load(recreate=False)  # 初回実行後にコメントアウトしてください

agent = Agent(
    knowledge_base=knowledge_base,
    search_knowledge=True,
)

agent.print_response("How to make Thai curry?", markdown=True)
```

## Agentic Chunking Params

<Snippet file="chunking-agentic.mdx" />