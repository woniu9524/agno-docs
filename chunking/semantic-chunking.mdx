---
title: セマンティックチャンキング
---

セマンティックチャンキングは、ドキュメントを埋め込みを使用してテキストセグメント間の意味的な類似性を分析することにより、より小さなチャンクに分割する方法です。設定可能な類似性しきい値に基づいて、意味が大きく変化する自然な区切り点を特定するために chonkie ライブラリを使用します。これにより、意味的に関連するコンテンツが同じチャンク内に留まるようにし、意味のあるトピック遷移で分割が発生するため、固定サイズのチャンキングよりもコンテキストと意味をより良く保持できます。

```python
from agno.agent import Agent
from agno.document.chunking.semantic import SemanticChunking
from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
from agno.vectordb.pgvector import PgVector

db_url = "postgresql+psycopg://ai:ai@localhost:5532/ai"

knowledge_base = PDFUrlKnowledgeBase(
    urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
    vector_db=PgVector(table_name="recipes_semantic_chunking", db_url=db_url),
    chunking_strategy=SemanticChunking(),
)
knowledge_base.load(recreate=False)  # 初回実行後にコメントアウトしてください

agent = Agent(
    knowledge_base=knowledge_base,
    search_knowledge=True,
)

agent.print_response("タイカレーの作り方は？", markdown=True)

```

## セマンティックチャンキングのパラメータ

<Snippet file="chunking-semantic.mdx" />