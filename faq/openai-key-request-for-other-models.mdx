---
title: 使用其他模型时请求OpenAI密钥
sidebarTitle: 使用其他模型时请求OpenAI密钥
---

如果您看到请求OpenAI API密钥的提示，但您并未明确配置OpenAI，这是因为Agno在多个地方默认使用了OpenAI模型，包括：

- 在`Agent`中未指定时的默认模型
- 除非另有指定，否则默认的嵌入器是OpenAIEmbedder配合VectorDBs

## 快速修复：配置其他模型

最好明确指定Agent的模型，否则它将默认使用`OpenAIChat`。

例如，要使用Google的Gemini而不是OpenAI：

```python
from agno.agent import Agent, RunResponse
from agno.models.google import Gemini

agent = Agent(
    model=Gemini(id="gemini-1.5-flash"),
    markdown=True,
)

# 在终端打印响应
agent.print_response("Share a 2 sentence horror story.")
```

有关配置不同模型提供商的更多详细信息，请查看我们的[模型文档](../models/)

## 快速修复：配置其他嵌入器

嵌入器也是同理。如果您想使用与`OpenAIEmbedder`不同的嵌入器，请明确配置它。

例如，要使用Google的Gemini作为嵌入器，请使用`GeminiEmbedder`：

```python
from agno.agent import AgentKnowledge
from agno.vectordb.pgvector import PgVector
from agno.embedder.google import GeminiEmbedder

# 将句子嵌入数据库
embeddings = GeminiEmbedder().get_embedding("The quick brown fox jumps over the lazy dog.")

# 打印嵌入及其维度
print(f"Embeddings: {embeddings[:5]}")
print(f"Dimensions: {len(embeddings)}")

# 在知识库中使用嵌入器
knowledge_base = AgentKnowledge(
    vector_db=PgVector(
        db_url="postgresql+psycopg://ai:ai@localhost:5532/ai",
        table_name="gemini_embeddings",
        embedder=GeminiEmbedder(),
    ),
    num_documents=2,
)
```

有关配置不同模型提供商的更多详细信息，请查看我们的[嵌入器文档](../embedder/)