### 使用 agno 进行身份验证

通过运行以下命令，使用 agno.com 进行身份验证：

```shell
ag setup
```

或者，通过从 [app.agno.com](https://app.agno.com/settings) 导出工作区的 `AGNO_API_KEY` 进行身份验证。

<CodeGroup>
```bash Mac
export AGNO_API_KEY=ag-***
```

```bash Windows
setx AGNO_API_KEY ag-***
```
</CodeGroup>