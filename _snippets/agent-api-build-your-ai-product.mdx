## AIプロダクトの構築

`agent-app` には、AIプロダクトの構築に使用できる一般的なエンドポイントが用意されています。このAPIは、実際のAIアプリと緊密に連携して開発されており、優れた出発点となります。

一般的なワークフローは以下のとおりです。

- フロントエンド/プロダクトは、エージェントを実行するために `POST /v1/agents/{agent_id}/runs` を呼び出します。
- 返された `session_id` を使用して、プロダクトはチャットを継続し、ユーザーに提供できます。