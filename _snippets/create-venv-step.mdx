<Step title="创建虚拟环境">
  打开 `Terminal` 并创建一个 Python 虚拟环境。

  <CodeGroup>

  ```bash Mac
  python3 -m venv .venv
  source .venv/bin/activate
  ```

  ```bash Windows
  python3 -m venv .venv
  .venv/scripts/activate
  ```

  </CodeGroup>
</Step>