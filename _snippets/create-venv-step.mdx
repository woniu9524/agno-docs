<Step title="仮想環境を作成する">
  `Terminal` を開き、Python の仮想環境を作成します。

  <CodeGroup>

  ```bash Mac
  python3 -m venv .venv
  source .venv/bin/activate
  ```

  ```bash Windows
  python3 -m venv .venv
  .venv/scripts/activate
  ```

  </CodeGroup>
</Step>