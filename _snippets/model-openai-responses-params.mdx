| 名前                          | タイプ                              | デフォルト              | 説明                                                                                  |
| ----------------------------- | --------------------------------- | -------------------- | ------------------------------------------------------------------------------------- |
| `id`                          | `str`                             | `"gpt-4o"`           | 使用するOpenAIモデルのID。                                                            |
| `name`                        | `str`                             | `"OpenAIResponses"`  | このレスポンスモデルインスタンスの名前。                                              |
| `provider`                    | `str`                             | `"OpenAI"`           | モデルのプロバイダー。                                                                |
| `include`                     | `Optional[List[str]]`             | `None`               | レスポンスに含めるレスポンスコンポーネントのリスト。                                  |
| `max_output_tokens`           | `Optional[int]`                   | `None`               | レスポンス出力で生成する最大トークン数。                                              |
| `metadata`                    | `Optional[Dict[str, Any]]`        | `None`               | リクエストに含める追加のメタデータ。                                                  |
| `parallel_tool_calls`         | `Optional[bool]`                  | `None`               | 並列ツール呼び出しを許可するかどうか。                                                |
| `reasoning`                   | `Optional[Dict[str, Any]]`        | `None`               | レスポンスでの推論/思考を有効化および制御するためのパラメータ。                       |
| `store`                       | `Optional[bool]`                  | `None`               | モデルの蒸留または評価のために、このレスポンスリクエストの出力を保存するかどうか。      |
| `temperature`                 | `Optional[float]`                 | `None`               | モデルの出力におけるランダム性を制御します。                                          |
| `top_p`                       | `Optional[float]`                 | `None`               |Nucleusサンプリングによる多様性を制御します。                                         |
| `truncation`                  | `Optional[str]`                   | `None`               | トークン制限を超えるコンテンツをどのように処理するか。                                |
| `user`                        | `Optional[str]`                   | `None`               | エンドユーザーを表す一意の識別子。                                                    |
| `response_format`             | `Optional[Any]`                   | `None`               | モデルが出力する必要がある形式を指定するオブジェクト。                                |
| `request_params`              | `Optional[Dict[str, Any]]`        | `None`               | リクエストに含める追加のパラメータ。                                                  |
| `api_key`                     | `Optional[str]`                   | `None`               | OpenAIとの認証に使用するAPIキー。                                                     |
| `organization`                | `Optional[str]`                   | `None`               | APIリクエストに使用する組織。                                                         |
| `base_url`                    | `Optional[Union[str, httpx.URL]]` | `None`               | APIリクエストのベースURL。                                                             |
| `timeout`                     | `Optional[float]`                 | `None`               | APIリクエストのタイムアウト。                                                         |
| `max_retries`                 | `Optional[int]`                   | `None`               | 失敗したリクエストの最大リトライ回数。                                                  |
| `default_headers`             | `Optional[Dict[str, str]]`        | `None`               | すべてのリクエストに含めるデフォルトヘッダー。                                        |
| `default_query`               | `Optional[Dict[str, str]]`        | `None`               | すべてのリクエストに含めるデフォルトクエリパラメータ。                                  |
| `http_client`                 | `Optional[httpx.Client]`          | `None`               | オプションの事前設定済みHTTPクライアント。                                            |
| `client_params`               | `Optional[Dict[str, Any]]`        | `None`               | クライアント設定のための追加パラメータ。                                              |
| `vector_store_name`           | `str`                             | `"knowledge_base"`   | ファイルアップロードと取得のためのベクトルストアの名前。                              |