---
title: 什么是推理？
sidebarTitle: 概述
description: 推理使代理能够“思考”后再响应，并“分析”其操作（例如工具调用）的结果，从而大大提高代理解决需要顺序工具调用的问题的能力。
---

推理代理在响应前会经历一个内部的思维链，尝试不同的想法，并进行验证和纠正。Agno 支持 3 种推理方法：
1. [推理模型](#reasoning-models)
2. [推理工具](#reasoning-tools)
3. [推理代理](#reasoning-agents)

哪种方法最适合取决于您的用例，我们建议您尝试所有方法，并沉浸在这个推理代理的新时代。

## 推理模型

推理模型是一类独立的、经过强化学习训练的、在回答前进行思考的大型语言模型。它们在响应前会产生一个内部的思维链。推理模型的例子包括 OpenAI o 系列、扩展思考模式下的 Claude 3.7 Sonnet、Gemini 2.0 Flash Thinking 和 DeepSeek-R1。

模型层面的推理主要关注模型在**开始生成响应之前**所做的事情。推理模型在单次调用用例中表现出色。它们非常适合解决不需要多次交互的难题（编码、数学、物理）或顺序调用工具。

### 示例

```python o3_mini.py
from agno.agent import Agent
from agno.models.openai import OpenAIChat

agent = Agent(model=OpenAIChat(id="o3-mini"))
agent.print_response(
    "解决电车难题。评估多种伦理框架。 "
    "包含解决方案的 ASCII 图。",
    stream=True,
)
```

在 [推理模型指南](/reasoning/reasoning-models) 中了解更多关于推理模型的信息。

## 推理模型 + 响应模型

如果我们想使用一个推理模型进行推理，但使用另一个模型来生成响应，会怎么样？众所周知，推理模型在解决问题方面非常出色，但在自然地响应方面（如 Claude Sonnet 或 GPT-4o）却不那么擅长。

通过为推理和响应使用不同的模型，我们可以兼顾两者的优点。

### 示例

让我们使用 Groq 的 deepseek-r1 进行推理，并使用 Claude Sonnet 进行自然响应。

```python deepseek_plus_claude.py
from agno.agent import Agent
from agno.models.anthropic import Claude
from agno.models.groq import Groq

deepseek_plus_claude = Agent(
    model=Claude(id="claude-3-7-sonnet-20250219"),
    reasoning_model=Groq(
        id="deepseek-r1-distill-llama-70b", temperature=0.6, max_tokens=1024, top_p=0.95
    ),
)
deepseek_plus_claude.print_response("9.11 和 9.9 -- 哪个更大？", stream=True)
```

## 推理工具

通过为模型提供一个 **“思考”工具**，我们可以通过提供一个专门的结构化思考空间来极大地提高其推理能力。这是一种简单而有效的方法，可以为非推理模型添加推理能力。

该研究最初由 Anthropic 在[这篇博文](https://www.anthropic.com/engineering/claude-think-tool)中发表，但在此之前已被许多 AI 工程师（包括我们自己的团队）实践。

### 示例

```python claude_thinking_tools.py
from agno.agent import Agent
from agno.models.anthropic import Claude
from agno.tools.thinking import ThinkingTools
from agno.tools.yfinance import YFinanceTools

reasoning_agent = Agent(
    model=Claude(id="claude-3-7-sonnet-latest"),
    tools=[
        ThinkingTools(add_instructions=True),
        YFinanceTools(
            stock_price=True,
            analyst_recommendations=True,
            company_info=True,
            company_news=True,
        ),
    ],
    instructions="尽可能使用表格",
    markdown=True,
)

if __name__ == "__main__":
    reasoning_agent.print_response(
        "撰写一份关于 NVDA 的报告。仅报告，无其他文本。",
        stream=True,
        show_full_reasoning=True,
        stream_intermediate_steps=True,
    )
```

在 [推理工具指南](/reasoning/reasoning-tools) 中了解更多关于推理工具的信息。

## 推理代理

推理代理是 Agno 开发的一种新型多代理系统，它将思维链推理与工具使用相结合。

您可以通过设置 `reasoning=True` 来为任何代理启用推理。

当一个启用了 `reasoning=True` 的代理被赋予任务时，一个单独的“推理代理”首先使用思维链来解决问题。在每一步，它都会调用工具来收集信息、验证结果并进行迭代，直到得出最终答案。一旦推理代理得出最终答案，它会将结果交还给原始代理进行验证并提供响应。

### 示例

```python reasoning_agent.py
from agno.agent import Agent
from agno.models.openai import OpenAIChat

reasoning_agent = Agent(
    model=OpenAIChat(id="gpt-4o"),
    reasoning=True,
    markdown=True,
)
reasoning_agent.print_response(
    "解决电车难题。评估多种伦理框架。 "
    "包含解决方案的 ASCII 图。",
    stream=True,
    show_full_reasoning=True,
)
```

在 [推理代理指南](/reasoning/reasoning-agents) 中了解更多关于推理代理的信息。