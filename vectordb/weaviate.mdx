---
title: Weaviate Agent Knowledge
sidebarTitle: Weaviate
---

Weaviate のセットアップについては、[Weaviate セットアップガイド](https://weaviate.io/developers/weaviate/quickstart)の手順に従ってください。

## セットアップ

weaviate パッケージをインストールします。

```shell
pip install weaviate-client
```

weaviate を実行します。

```shell
docker run -d \
-p 8080:8080 \
-p 50051:50051 \
--name weaviate \
cr.weaviate.io/semitechnologies/weaviate:1.28.4
```

または

```shell
./cookbook/scripts/run_weaviate.sh
```

## 例

```python agent_with_knowledge.py
from agno.agent import Agent
from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
from agno.vectordb.search import SearchType
from agno.vectordb.weaviate import Distance, VectorIndex, Weaviate

vector_db = Weaviate(
    collection="recipes",
    search_type=SearchType.hybrid,
    vector_index=VectorIndex.HNSW,
    distance=Distance.COSINE,
    local=True,  # Weaviate Cloud を使用する場合は False に設定し、ローカルインスタンスを使用する場合は True に設定します
)
# ナレッジベースを作成します
knowledge_base = PDFUrlKnowledgeBase(
    urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
    vector_db=vector_db,
)
knowledge_base.load(recreate=False)  # 初回実行後にコメントアウトしてください

# エージェントを作成して使用します
agent = Agent(
    knowledge=knowledge_base,
    search_knowledge=True,
    show_tool_calls=True,
)
agent.print_response("How to make Thai curry?", markdown=True)
```

<Card title="Async Support ⚡">
  <div className="mt-2">
    <p>
      Weaviate は非同期操作もサポートしており、並行処理を可能にし、パフォーマンスを向上させます。
    </p>

    ```python async_weaviate_db.py
    import asyncio

    from agno.agent import Agent
    from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
    from agno.vectordb.search import SearchType
    from agno.vectordb.weaviate import Distance, VectorIndex, Weaviate

    vector_db = Weaviate(
        collection="recipes_async",
        search_type=SearchType.hybrid,
        vector_index=VectorIndex.HNSW,
        distance=Distance.COSINE,
        local=True,  # Weaviate Cloud を使用する場合は False に設定し、ローカルインスタンスを使用する場合は True に設定します
    )

    # ナレッジベースを作成します
    knowledge_base = PDFUrlKnowledgeBase(
        urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
        vector_db=vector_db,
    )

    agent = Agent(
        knowledge=knowledge_base,
        search_knowledge=True,
        show_tool_calls=True,
    )

    if __name__ == "__main__":
        # 初回実行後にコメントアウトしてください
        asyncio.run(knowledge_base.aload(recreate=False))

        # エージェントを作成して使用します
        asyncio.run(agent.aprint_response("How to make Tom Kha Gai", markdown=True))
    ```

    <Tip className="mt-4">
      Weaviate の非同期機能は <code>WeaviateAsyncClient</code> を活用して、ノンブロッキングなベクトル操作を提供します。これは、高い並行処理とスループットを必要とするアプリケーションに特に価値があります。
    </Tip>
  </div>
</Card>

## Weaviate パラメータ

<Snippet file="vectordb_weaviate_params.mdx" />

## 開発者向けリソース

- [クックブック (同期)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/weaviate_db/weaviate_db.py) を表示
- [クックブック (非同期)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/weaviate_db/async_weaviate_db.py) を表示