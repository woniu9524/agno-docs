---
title: Clickhouse Agent Knowledge
sidebarTitle: Clickhouse
---

## セットアップ

```shell
docker run -d \
  -e CLICKHOUSE_DB=ai \
  -e CLICKHOUSE_USER=ai \
  -e CLICKHOUSE_PASSWORD=ai \
  -e CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1 \
  -v clickhouse_data:/var/lib/clickhouse/ \
  -v clickhouse_log:/var/log/clickhouse-server/ \
  -p 8123:8123 \
  -p 9000:9000 \
  --ulimit nofile=262144:262144 \
  --name clickhouse-server \
  clickhouse/clickhouse-server
```

## 例

```python agent_with_knowledge.py
from agno.agent import Agent
from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
from agno.storage.sqlite import SqliteStorage
from agno.vectordb.clickhouse import Clickhouse

agent = Agent(
    storage=SqliteStorage(table_name="recipe_agent"),
    knowledge=PDFUrlKnowledgeBase(
        urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
        vector_db=Clickhouse(
            table_name="recipe_documents",
            host="localhost",
            port=8123,
            username="ai",
            password="ai",
        ),
    ),
    # ツールコールをレスポンスに表示
    show_tool_calls=True,
    # エージェントがナレッジベースを検索できるようにする
    search_knowledge=True,
    # エージェントがチャット履歴を読み取れるようにする
    read_chat_history=True,
)
# 初回実行後にコメントアウト
agent.knowledge.load(recreate=False)  # type: ignore

agent.print_response("How do I make pad thai?", markdown=True)
agent.print_response("What was my last question?", stream=True)
```

<Card title="Async Support ⚡">
  <div className="mt-2">
    <p>
      Clickhouseは非同期操作もサポートしており、並行処理を可能にし、パフォーマンスを向上させます。
    </p>
    
    ```python async_clickhouse.py
    import asyncio

    from agno.agent import Agent
    from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
    from agno.storage.agent.sqlite import SqliteAgentStorage
    from agno.vectordb.clickhouse import Clickhouse

    agent = Agent(
        storage=SqliteAgentStorage(table_name="recipe_agent"),
        knowledge=PDFUrlKnowledgeBase(
            urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
            vector_db=Clickhouse(
                table_name="recipe_documents",
                host="localhost",
                port=8123,
                username="ai",
                password="ai",
            ),
        ),
        # ツールコールをレスポンスに表示
        show_tool_calls=True,
        # エージェントがナレッジベースを検索できるようにする
        search_knowledge=True,
        # エージェントがチャット履歴を読み取れるようにする
        read_chat_history=True,
    )

    if __name__ == "__main__":
        # 初回実行後にコメントアウト
        asyncio.run(agent.knowledge.aload(recreate=False))

        # エージェントを作成して使用する
        asyncio.run(agent.aprint_response("How to make Tom Kha Gai", markdown=True))
    ```
    
    <Tip className="mt-4">
      高スループットアプリケーションでノンブロッキング操作を行うには、<code>asyncio.run()</code>と共に<code>aload()</code>および<code>aprint_response()</code>メソッドを使用してください。
    </Tip>
  </div>
</Card>

## 開発者向けリソース

- [Cookbook (Sync)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/clickhouse_db/clickhouse.py)を表示
- [Cookbook (Async)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/clickhouse_db/async_clickhouse.py)を表示