---
title: Pinecone Agent Knowledge
sidebarTitle: Pinecone
---

## セットアップ

Pinecone をすぐに使い始めるには、[Pinecone セットアップ ガイド](https://docs.pinecone.io/guides/get-started/quickstart)の手順に従ってください。

```shell
pip install pinecone
```

<Info>
  Pinecone v6.x.x はまだサポートされていません。互換性を確保するために積極的に取り組んでいます。それまでの間、最良の体験を得るために **Pinecone v5.4.2** の使用をお勧めします。
</Info>

## 例

```python agent_with_knowledge.py
import os
import typer
from typing import Optional
from rich.prompt import Prompt

from agno.agent import Agent
from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
from agno.vectordb.pineconedb import PineconeDb

api_key = os.getenv("PINECONE_API_KEY")
index_name = "thai-recipe-hybrid-search"

vector_db = PineconeDb(
    name=index_name,
    dimension=1536,
    metric="cosine",
    spec={"serverless": {"cloud": "aws", "region": "us-east-1"}},
    api_key=api_key,
    use_hybrid_search=True,
    hybrid_alpha=0.5,
)

knowledge_base = PDFUrlKnowledgeBase(
    urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
    vector_db=vector_db,
)

def pinecone_agent(user: str = "user"):
    run_id: Optional[str] = None

    agent = Agent(
        run_id=run_id,
        user_id=user,
        knowledge=knowledge_base,
        show_tool_calls=True,
        debug_mode=True,
    )

    if run_id is None:
        run_id = agent.run_id
        print(f"Started Run: {run_id}\n")
    else:
        print(f"Continuing Run: {run_id}\n")

    while True:
        message = Prompt.ask(f"[bold] :sunglasses: {user} [/bold]")
        if message in ("exit", "bye"):
            break
        agent.print_response(message)

if __name__ == "__main__":
    # 初回実行後にコメントアウトしてください
    knowledge_base.load(recreate=True, upsert=True)

    typer.run(pinecone_agent)
```

<Card title="非同期サポート ⚡">
  <div className="mt-2">
    <p>
      Pinecone は非同期操作もサポートしており、並列処理を可能にし、パフォーマンスを向上させます。
    </p>
    
    ```python async_pinecone.py
    import asyncio
    from os import getenv

    from agno.agent import Agent
    from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
    from agno.vectordb.pineconedb import PineconeDb

    api_key = getenv("PINECONE_API_KEY")
    index_name = "thai-recipe-index"

    vector_db = PineconeDb(
        name=index_name,
        dimension=1536,
        metric="cosine",
        spec={"serverless": {"cloud": "aws", "region": "us-east-1"}},
        api_key=api_key,
    )

    knowledge_base = PDFUrlKnowledgeBase(
        urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
        vector_db=vector_db,
    )

    agent = Agent(
        knowledge=knowledge_base,
        # レスポンスでツール呼び出しを表示する
        show_tool_calls=True,
        # エージェントがナレッジベースを検索できるようにする
        search_knowledge=True,
        # エージェントがチャット履歴を読み取れるようにする
        read_chat_history=True,
    )

    if __name__ == "__main__":
        # 初回実行後にコメントアウトしてください
        asyncio.run(knowledge_base.aload(recreate=False, upsert=True))

        # エージェントを作成して使用する
        asyncio.run(agent.aprint_response("How to make Tom Kha Gai", markdown=True))
    ```
    
    <Tip className="mt-4">
      高スループットアプリケーションでノンブロッキング操作を行うには、<code>asyncio.run()</code> と共に <code>aload()</code> および <code>aprint_response()</code> メソッドを使用してください。
    </Tip>
  </div>
</Card>

## PineconeDb パラメータ

<Snippet file="vectordb_pineconedb_params.mdx" />

## 開発者向けリソース

- [クックブック (同期)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/pinecone_db/pinecone_db.py) を表示
- [クックブック (非同期)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/pinecone_db/async_pinecone_db.py) を表示