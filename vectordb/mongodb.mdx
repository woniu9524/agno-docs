---
title: MongoDB Agent Knowledge
sidebarTitle: MongoDB
---

## セットアップ

接続文字列を取得するには、[MongoDB セットアップ ガイド](https://www.mongodb.com/docs/atlas/getting-started/)の手順に従ってください。

MongoDB パッケージのインストール

```shell
pip install "pymongo[srv]"
```

## 例

```python agent_with_knowledge.py
from agno.agent import Agent
from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
from agno.vectordb.mongodb import MongoDb

# MongoDB Atlas 接続文字列
"""
接続文字列の例:
"mongodb+srv://<username>:<password>@cluster0.mongodb.net/?retryWrites=true&w=majority"
"mongodb://localhost/?directConnection=true"
"""
mdb_connection_string = ""

knowledge_base = PDFUrlKnowledgeBase(
    urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
    vector_db=MongoDb(
        collection_name="recipes",
        db_url=mdb_connection_string,
        wait_until_index_ready=60,
        wait_after_insert=300
    ),
)  # wait_after_insert および wait_until_index_ready を必要に応じて調整してください

# knowledge_base.load(recreate=True)  # 初回実行後にコメントアウトしてください

agent = Agent(knowledge=knowledge_base, show_tool_calls=True)
agent.print_response("タイカレーの作り方は？", markdown=True)
```

<Card title="非同期サポート ⚡">
  <div className="mt-2">
    <p>
      MongoDB は非同期操作もサポートしており、並行処理を可能にし、パフォーマンスを向上させます。
    </p>
    
    ```python async_mongodb.py
    import asyncio

    from agno.agent import Agent
    from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
    from agno.vectordb.mongodb import MongoDb

    # MongoDB Atlas 接続文字列
    """
    接続文字列の例:
    "mongodb+srv://<username>:<password>@cluster0.mongodb.net/?retryWrites=true&w=majority"
    "mongodb://localhost:27017/agno?authSource=admin"
    """
    mdb_connection_string = "mongodb+srv://<username>:<password>@cluster0.mongodb.net/?retryWrites=true&w=majority"

    knowledge_base = PDFUrlKnowledgeBase(
        urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
        vector_db=MongoDb(
            collection_name="recipes",
            db_url=mdb_connection_string,
        ),
    )

    # エージェントの作成と使用
    agent = Agent(knowledge=knowledge_base, show_tool_calls=True)

    if __name__ == "__main__":
        # 初回実行後にコメントアウトしてください
        asyncio.run(knowledge_base.aload(recreate=False))

        asyncio.run(agent.aprint_response("タイカレーの作り方は？", markdown=True))
    ```
    
    <Tip className="mt-4">
      高スループットアプリケーションでの非ブロッキング操作には、<code>asyncio.run()</code> と共に <code>aload()</code> および <code>aprint_response()</code> メソッドを使用してください。
    </Tip>
  </div>
</Card>

## MongoDB パラメータ

<Snippet file="vectordb_mongodb_params.mdx" />

## 開発者向けリソース

- [クックブック (同期)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/mongo_db/mongo_db.py) を表示
- [クックブック (非同期)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/mongo_db/async_mongo_db.py) を表示