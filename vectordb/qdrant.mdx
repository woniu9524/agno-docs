---
title: Qdrant Agent Knowledge
sidebarTitle: Qdrant
---

## セットアップ

Qdrant をローカルにインストールするには、[Qdrant セットアップ ガイド](https://qdrant.tech/documentation/guides/installation/) の手順に従ってください。API キーを取得するためのガイドは次のとおりです。[Qdrant API キー](https://qdrant.tech/documentation/cloud/authentication/)。

## 例

```python agent_with_knowledge.py
import os
import typer
from typing import Optional
from rich.prompt import Prompt

from agno.agent import Agent
from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
from agno.vectordb.qdrant import Qdrant

api_key = os.getenv("QDRANT_API_KEY")
qdrant_url = os.getenv("QDRANT_URL")
collection_name = "thai-recipe-index"

vector_db = Qdrant(
    collection=collection_name,
    url=qdrant_url,
    api_key=api_key,
)

knowledge_base = PDFUrlKnowledgeBase(
    urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
    vector_db=vector_db,
)

def qdrant_agent(user: str = "user"):
    run_id: Optional[str] = None

    agent = Agent(
        run_id=run_id,
        user_id=user,
        knowledge=knowledge_base,
        tool_calls=True,
        use_tools=True,
        show_tool_calls=True,
        debug_mode=True,
    )

    if run_id is None:
        run_id = agent.run_id
        print(f"Started Run: {run_id}\n")
    else:
        print(f"Continuing Run: {run_id}\n")

    while True:
        message = Prompt.ask(f"[bold] :sunglasses: {user} [/bold]")
        if message in ("exit", "bye"):
            break
        agent.print_response(message)

if __name__ == "__main__":
    # 初回実行後にコメントアウトしてください
    knowledge_base.load(recreate=True, upsert=True)

    typer.run(qdrant_agent)
```

<Card title="非同期サポート ⚡">
  <div className="mt-2">
    <p>
      Qdrant は非同期操作もサポートしており、並行処理を可能にし、パフォーマンスを向上させます。
    </p>
    
    ```python async_qdrant_db.py
    import asyncio

    from agno.agent import Agent
    from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
    from agno.vectordb.qdrant import Qdrant

    COLLECTION_NAME = "thai-recipes"

    # ローカルインスタンスで Qdrant を初期化
    vector_db = Qdrant(
        collection=COLLECTION_NAME, 
        url="http://localhost:6333"
    )

    # ナレッジベースを作成
    knowledge_base = PDFUrlKnowledgeBase(
        urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
        vector_db=vector_db,
    )

    agent = Agent(knowledge=knowledge_base, show_tool_calls=True)

    if __name__ == "__main__":
        # ナレッジベースを非同期でロード
        asyncio.run(knowledge_base.aload(recreate=False))  # 初回実行後にコメントアウトしてください

        # エージェントを非同期で作成および使用
        asyncio.run(agent.aprint_response("How to make Tom Kha Gai", markdown=True))
    ```
    
    <Tip className="mt-4">
      <code>aload()</code> および <code>aprint_response()</code> を asyncio と共に使用すると、非ブロッキング操作が提供され、負荷下でのアプリケーションの応答性が向上します。
    </Tip>
  </div>
</Card>

## Qdrant パラメータ

<Snippet file="vectordb_qdrant_params.mdx" />

## 開発者向けリソース

- [クックブック (同期)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/qdrant_db/qdrant_db.py) を表示
- [クックブック (非同期)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/qdrant_db/async_qdrant_db.py) を表示