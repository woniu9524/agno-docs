---
title: ChromaDB Agent Knowledge
sidebarTitle: ChromaDB
---

## セットアップ

```shell
pip install chromadb
```

## 例

```python agent_with_knowledge.py
import typer
from rich.prompt import Prompt
from typing import Optional

from agno.agent import Agent
from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
from agno.vectordb.chroma import ChromaDb

knowledge_base = PDFUrlKnowledgeBase(
    urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
    vector_db=ChromaDb(collection="recipes"),
)

def pdf_agent(user: str = "user"):
    run_id: Optional[str] = None

    agent = Agent(
        run_id=run_id,
        user_id=user,
        knowledge_base=knowledge_base,
        use_tools=True,
        show_tool_calls=True,
        debug_mode=True,
    )
    if run_id is None:
        run_id = agent.run_id
        print(f"Started Run: {run_id}\n")
    else:
        print(f"Continuing Run: {run_id}\n")

    while True:
        message = Prompt.ask(f"[bold] :sunglasses: {user} [/bold]")
        if message in ("exit", "bye"):
            break
        agent.print_response(message)

if __name__ == "__main__":
    # 初回実行後にコメントアウトしてください
    knowledge_base.load(recreate=False)

    typer.run(pdf_agent)
```

<Card title="非同期サポート ⚡">
  <div className="mt-2">
    <p>
      ChromaDBは非同期操作もサポートしており、並行処理を可能にし、パフォーマンスを向上させます。
    </p>
    
    ```python async_chroma_db.py
    # chromadb をインストールします - `pip install chromadb`

    import asyncio

    from agno.agent import Agent
    from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
    from agno.vectordb.chroma import ChromaDb

    # ChromaDB を初期化します
    vector_db = ChromaDb(collection="recipes", path="tmp/chromadb", persistent_client=True)

    # ナレッジベースを作成します
    knowledge_base = PDFUrlKnowledgeBase(
        urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
        vector_db=vector_db,
    )

    # エージェントを作成して使用します
    agent = Agent(knowledge=knowledge_base, show_tool_calls=True)

    if __name__ == "__main__":
        # 初回実行後にコメントアウトしてください
        asyncio.run(knowledge_base.aload(recreate=False))

        # エージェントを作成して使用します
        asyncio.run(agent.aprint_response("How to make Tom Kha Gai", markdown=True))
    ```
    
    <Tip className="mt-4">
      高スループットアプリケーションで非ブロッキング操作を行うには、<code>asyncio.run()</code> と共に <code>aload()</code> および <code>aprint_response()</code> メソッドを使用してください。
    </Tip>
  </div>
</Card>

## ChromaDb パラメータ

<Snippet file="vectordb_chromadb_params.mdx" />

## 開発者向けリソース

- [クックブック (同期)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/chroma_db/chroma_db.py) を表示
- [クックブック (非同期)](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/vector_dbs/chroma_db/async_chroma_db.py) を表示