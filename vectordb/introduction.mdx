---
title: 什么是向量数据库？
sidebarTitle: 概述
description: 向量数据库使我们能够将信息存储为嵌入，并使用余弦相似度或全文搜索来搜索与输入查询“相似”的结果。然后，这些结果作为上下文提供给 Agent，以便它能够使用检索增强生成 (RAG) 以上下文感知的方式进行响应。
---

以下是向量数据库与 Agent 的使用方式：

<Steps>
  <Step title="分块信息">
    将知识分解成更小的块，以确保我们的搜索查询
    只返回相关结果。
  </Step>
  <Step title="加载知识库">
    将块转换为嵌入向量并将其存储在向量数据库中。
  </Step>
  <Step title="搜索知识库">
    当用户发送消息时，我们将输入消息转换为嵌入，并在向量数据库中“搜索”最近邻。
  </Step>
</Steps>

许多向量数据库也支持混合搜索，它将向量相似性搜索的功能与传统的基于关键字的搜索相结合。这种方法可以显著提高搜索结果的相关性和准确性，尤其是在处理复杂查询或处理不同类型数据时。

混合搜索通常通过以下方式工作：

1. 执行向量相似性搜索以查找语义上相似的内容。
2. 进行基于关键字的搜索以识别精确或近似匹配。
3. 使用加权方法组合结果以提供最相关的信息。

这种能力允许更灵活和强大的查询，通常比单独使用任何一种方法都能产生更好的结果。

<Card title="⚡ 异步操作">
  <p>多个向量数据库支持异步操作，通过非阻塞操作、并发处理、降低延迟以及与 FastAPI 和异步 Agent 的无缝集成来提高性能。</p>
  <Tip className="mt-4">
    使用 Agno 进行构建时，请在生产环境中使用 <code>aload</code> 方法进行异步知识库加载。
  </Tip>
</Card>

## 支持的向量数据库

目前支持以下 VectorDb：

- [PgVector](/vectordb/pgvector)\*
- [Cassandra](/vectordb/cassandra)
- [ChromaDb](/vectordb/chroma)
- [Couchbase](/vectordb/couchbase)\*
- [Clickhouse](/vectordb/clickhouse)
- [LanceDb](/vectordb/lancedb)\*
- [Milvus](/vectordb/milvus)
- [MongoDb](/vectordb/mongodb)
- [Pinecone](/vectordb/pinecone)\*
- [Qdrant](/vectordb/qdrant)
- [Singlestore](/vectordb/singlestore)
- [Weaviate](/vectordb/weaviate)

\*支持混合搜索

这些数据库各有优缺点和特性，包括对混合搜索和异步操作的支持程度不同。请务必查看每个数据库的特定文档，以了解如何在项目中最佳地利用其功能。