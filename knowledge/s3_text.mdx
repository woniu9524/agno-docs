---
title: S3 テキストナレッジベース
description: S3 バケットからナレッジベースにテキストファイルを使用する方法を学びます。
sidebarTitle: S3 テキスト
---

**S3TextKnowledgeBase** は、S3 バケットから **テキスト** ファイルを読み込み、それらをベクトル埋め込みに変換して、ベクトルデータベースにロードします。

## 使用方法

<Note>

この例では、ローカルの PgVector データベースを使用しています。[実行されていることを確認してください](https://docs.agno.com/vectordb/pgvector)

</Note>

```shell
pip install textract
```

```python
from agno.knowledge.s3.text import S3TextKnowledgeBase
from agno.vectordb.pgvector import PgVector

db_url = "postgresql+psycopg://ai:ai@localhost:5532/ai"

knowledge_base = S3TextKnowledgeBase(
    bucket_name="agno-public",
    key="recipes/recipes.docx",
    vector_db=PgVector(table_name="recipes", db_url=db_url),
)
```

次に、`Agent` で `knowledge_base` を使用します。

```python
from agno.agent import Agent
from knowledge_base import knowledge_base

agent = Agent(
    knowledge=knowledge_base,
    search_knowledge=True,
)
agent.knowledge.load(recreate=False)

agent.print_response("How to make Hummus?")
```

## パラメータ

| パラメータ     | タイプ           | デフォルト             | 説明                                                                               |
| ------------- | -------------- | ------------------- | ----------------------------------------------------------------------------------------- |
| `bucket_name` | `str`          | `None`              | ファイルが存在する S3 バケットの名前。                                            |
| `key`         | `str`          | `None`              | バケット内のファイルのキー。                                                        |
| `formats`     | `List[str]`    | `[".doc", ".docx"]` | このナレッジベースで受け入れられるフォーマット。                                                  |
| `reader`      | `S3TextReader` | `S3TextReader()`    | ベクトルデータベースの `Documents` に `Text` ファイルを変換する `S3TextReader`。 |

`S3TextKnowledgeBase` は [AgentKnowledge](/reference/knowledge/base) クラスのサブクラスであり、同じパラメータにアクセスできます。

## 開発者向けリソース

- [クックブック](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/s3_text_kb.py) を表示