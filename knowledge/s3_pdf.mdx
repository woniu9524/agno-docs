---
title: S3 PDF ナレッジベース
description: S3 バケットから PDF をナレッジベースで使用する方法を学びます。
sidebarTitle: S3 PDF
---

**S3PDFKnowledgeBase** は、S3 バケットから **PDF** ファイルを読み込み、それらをベクトル埋め込みに変換して、ベクトルデータベースにロードします。

## 使用方法

<Note>

この例では、ローカルの PgVector データベースを使用しています。[実行されていることを確認してください](https://docs.agno.com/vectordb/pgvector)

</Note>

```python
from agno.knowledge.s3.pdf import S3PDFKnowledgeBase
from agno.vectordb.pgvector import PgVector

db_url = "postgresql+psycopg://ai:ai@localhost:5532/ai"

knowledge_base = S3PDFKnowledgeBase(
    bucket_name="agno-public",
    key="recipes/ThaiRecipes.pdf",
    vector_db=PgVector(table_name="recipes", db_url=db_url),
)
```

次に、`Agent` で `knowledge_base` を使用します。

```python
from agno.agent import Agent
from knowledge_base import knowledge_base

agent = Agent(
    knowledge=knowledge_base,
    search_knowledge=True,
)
agent.knowledge.load(recreate=False)

agent.print_response("How to make Thai curry?")
```

## パラメータ

| パラメータ     | タイプ          | デフォルト         | 説明                                                                        |
| ------------- | ------------- | --------------- | ---------------------------------------------------------------------------------- |
| `bucket_name` | `str`         | `None`          | PDF が格納されている S3 バケットの名前。                                      |
| `key`         | `str`         | `None`          | バケット内の PDF ファイルのキー。                                             |
| `reader`      | `S3PDFReader` | `S3PDFReader()` | ベクトルデータベース用の `PDF` を `Documents` に変換する `S3PDFReader`。 |

`S3PDFKnowledgeBase` は [AgentKnowledge](/reference/knowledge/base) クラスのサブクラスであり、同じパラメータにアクセスできます。

## 開発者向けリソース

- [クックブック](https://github.com/agno-agi/agno/blob/main/cookbook/agent_concepts/knowledge/s3_pdf_kb.py) を表示