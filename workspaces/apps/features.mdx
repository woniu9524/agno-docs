---
title: Features
---

## インストール要件の起動時インストール

アプリはコンテナ起動時に要件をインストールできます。`Jupyter` アプリを以下のように更新してください。

```python resources.py
...
# -*- Jupyter running on port 8888:8888
jupyter = Jupyter(
    mount_workspace=True,
    install_requirements=True,
    requirements_file="requirements.txt",
    env_vars={"OPENAI_API_KEY": getenv("OPENAI_API_KEY")},
)
...
```

同じディレクトリに `requirements.txt` ファイルを作成してください。

```python requirements.txt
openai
```

## リソースのパッチ適用

<CodeGroup>

```bash terminal
ag patch resources.py -y
```

```bash full options
ag patch resources.py --yes
```

</CodeGroup>