---
title: Human in the Loop
---

この例では、エージェントワークフローに人間の検証を実装する方法を示します。これは以下を実証します：
- 実行前の検証
- 実行後のレビュー
- 対話的なフィードバックループ
- 品質管理チェックポイント

例となるシナリオ：
- コンテンツモデレーション
- クリティカルな意思決定の承認
- 出力品質の検証
- 安全チェック
- 専門家レビュープロセス

## コード

```python human_in_the_loop.py
import json
from textwrap import dedent
from typing import Iterator

import httpx
from agno.agent import Agent
from agno.exceptions import StopAgentRun
from agno.tools import FunctionCall, tool
from rich.console import Console
from rich.pretty import pprint
from rich.prompt import Prompt

# これは、print_response メソッドによって使用されるコンソールインスタンスです。
# これを使用して、ライブ表示を停止および再開し、ユーザーに確認を求めることができます。
console = Console()


def pre_hook(fc: FunctionCall):
    # コンソールからライブ表示インスタンスを取得します
    live = console._live

    # ユーザーに確認を求めるために、ライブ表示を一時的に停止します
    live.stop()  # type: ignore

    # 確認を求めます
    console.print(f"\nAbout to run [bold blue]{fc.function.name}[/]")
    message = (
        Prompt.ask("Do you want to continue?", choices=["y", "n"], default="y")
        .strip()
        .lower()
    )

    # ライブ表示を再開します
    live.start()  # type: ignore

    # ユーザーが続行しない場合、StopExecution 例外を発生させます
    if message != "y":
        raise StopAgentRun(
            "Tool call cancelled by user",
            agent_message="Stopping execution as permission was not granted.",
        )


@tool(pre_hook=pre_hook)
def get_top_hackernews_stories(num_stories: int) -> Iterator[str]:
    """ユーザー確認後に Hacker News からトップストーリーを取得します。

    Args:
        num_stories (int): 取得するストーリーの数

    Returns:
        str: ストーリーの詳細を含む JSON 文字列
    """
    # トップストーリー ID を取得します
    response = httpx.get("https://hacker-news.firebaseio.com/v0/topstories.json")
    story_ids = response.json()

    # ストーリーの詳細を yield します
    for story_id in story_ids[:num_stories]:
        story_response = httpx.get(
            f"https://hacker-news.firebaseio.com/v0/item/{story_id}.json"
        )
        story = story_response.json()
        if "text" in story:
            story.pop("text", None)
        yield json.dumps(story)


# テクノロジーに精通した個性と明確な指示でエージェントを初期化します
agent = Agent(
    description="A Tech News Assistant that fetches and summarizes Hacker News stories",
    instructions=dedent("""\
        You are an enthusiastic Tech Reporter

        Your responsibilities:
        - Present Hacker News stories in an engaging and informative way
        - Provide clear summaries of the information you gather

        Style guide:
        - Use emoji to make your responses more engaging
        - Keep your summaries concise but informative
        - End with a friendly tech-themed sign-off\
    """),
    tools=[get_top_hackernews_stories],
    show_tool_calls=True,
    markdown=True,
)

# 試すための質問例：
# - "What are the top 3 HN stories right now?"
# - "Show me the most recent story from Hacker News"
# - "Get the top 5 stories (you can try accepting and declining the confirmation)"
agent.print_response(
    "What are the top 2 hackernews stories?", stream=True, console=console
)
```

## 使用方法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install libraries">
    ```bash
    pip install openai agno
    ```
  </Step>

  <Step title="Run the agent">
    ```bash
    python human_in_the_loop.py
    ```
  </Step>

</Steps>