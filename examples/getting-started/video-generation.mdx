---
title: 视频生成
---

本示例展示了如何创建一个使用 ModelsLabs 的 AI 代理来生成视频。您可以使用此代理创建各种类型的短视频，从动画场景到创意视觉故事。

可尝试的示例提示：
- "创作一段日落时海滩上波涛汹涌的宁静视频"
- "生成一段魔法森林中蝴蝶飞舞的魔法视频"
- "创作一段花园里花朵绽放的延时视频"
- "生成一段夜空中舞动的北极光的视频"

## 代码

```python video_generation.py
from textwrap import dedent

from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.models_labs import ModelsLabTools

# 创建一个创意 AI 视频导演代理
video_agent = Agent(
    model=OpenAIChat(id="gpt-4o"),
    tools=[ModelsLabTools()],
    description=dedent("""\
        您是一位经验丰富的 AI 视频导演，精通各种视频风格，
        从自然场景到艺术动画。您对通过视频内容进行的运动、
        时机和视觉叙事有深刻的理解。\
    """),
    instructions=dedent("""\
        作为一名 AI 视频导演，请遵循以下指南：
        1. 仔细分析用户的请求，理解所需的风格和情绪
        2. 生成前，通过添加运动、时机和氛围的细节来增强提示
        3. 使用 `generate_media` 工具，并提供详细、精心制作的提示
        4. 对所做的创意选择进行简要解释
        5. 如果请求不明确，请就风格偏好进行澄清

        视频将在您的响应下方自动显示在 UI 中。
        始终致力于创作引人入胜且有意义的视频，将用户的愿景变为现实！\
    """),
    markdown=True,
    show_tool_calls=True,
)

# 示例用法
video_agent.print_response(
    "生成一段穿越彩色星云的宇宙之旅", stream=True
)

# 检索并显示生成的视频
videos = video_agent.get_videos()
if videos:
    for video in videos:
        print(f"生成的视频 URL: {video.url}")

# 更多可尝试的示例提示：
"""
尝试这些创意提示：
1. "创作一段宁静森林中秋叶飘落的视频"
2. "生成一段猫咪玩球的视频"
3. "创作一段水波荡漾的宁静锦鲤池的视频"
4. "生成一段有舞动火焰的舒适壁炉视频"
5. "创作一段神秘领域中神秘传送门开启的视频"
"""
```

## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="安装库">
    ```bash
    pip install openai agno
    ```
  </Step>

  <Step title="设置环境变量">
    ```bash
    export MODELS_LAB_API_KEY=****
    ```
  </Step>

  <Step title="运行代理">
    ```bash
    python video_generation.py
    ```
  </Step>

</Steps>