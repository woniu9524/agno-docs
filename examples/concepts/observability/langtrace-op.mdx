---
title: Langtrace
---

## 概述

本示例演示了如何为您的 Agno Agent 集成 Langtrace 以进行跟踪和监控。

## 代码

```python
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.yfinance import YFinanceTools
from langtrace_python_sdk import langtrace
from langtrace_python_sdk.utils.with_root_span import with_langtrace_root_span

# 初始化 Langtrace
langtrace.init()

# 创建并配置 Agent
agent = Agent(
    name="Stock Price Agent",
    model=OpenAIChat(id="gpt-4o-mini"),
    tools=[YFinanceTools()],
    instructions="You are a stock price agent. Answer questions in the style of a stock analyst.",
    debug_mode=True,
)

# 使用 Agent
agent.print_response("What is the current price of Tesla?")
```

## 用法

<Steps>
  <Step title="安装依赖">
    ```bash
    pip install agno openai langtrace-python-sdk
    ```
  </Step>

  <Step title="设置环境变量">
    ```bash
    export LANGTRACE_API_KEY=<your-key>
    ```
  </Step>

  <Step title="运行 Agent">
    <CodeGroup>
    ```bash Mac
    python cookbook/observability/langtrace_op.py
    ```

    ```bash Windows
    python cookbook/observability/langtrace_op.py
    ```
    </CodeGroup>
  </Step>
</Steps>

## 注意事项

- **初始化**: 在使用 Agent 之前，请调用 `langtrace.init()` 来初始化 Langtrace。