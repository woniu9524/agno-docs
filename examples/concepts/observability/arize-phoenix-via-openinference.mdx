---
title: Arize Phoenix via OpenInference
---

## 概述

本示例演示了如何使用 OpenInference instrument 您的 Agno agent，并将 traces 发送到 Arize Phoenix。

## 代码

```python
import asyncio
import os

from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.yfinance import YFinanceTools
from phoenix.otel import register

# 设置 Arize Phoenix 的环境变量
os.environ["PHOENIX_CLIENT_HEADERS"] = f"api_key={os.getenv('ARIZE_PHOENIX_API_KEY')}"
os.environ["PHOENIX_COLLECTOR_ENDPOINT"] = "https://app.phoenix.arize.com"

# 配置 Phoenix tracer
tracer_provider = register(
    project_name="agno-stock-price-agent",  # 默认为 'default'
    auto_instrument=True,  # 自动使用已安装的 OpenInference instrumentation
)

# 创建并配置 agent
agent = Agent(
    name="Stock Price Agent",
    model=OpenAIChat(id="gpt-4o-mini"),
    tools=[YFinanceTools()],
    instructions="You are a stock price agent. Answer questions in the style of a stock analyst.",
    debug_mode=True,
)

# 使用 agent
agent.print_response("What is the current price of Tesla?")
```

## 使用方法

<Steps>
  <Step title="安装依赖">
    ```bash
    pip install agno arize-phoenix openai openinference-instrumentation-agno opentelemetry-sdk opentelemetry-exporter-otlp
    ```
  </Step>

  <Step title="设置环境变量">
    ```bash
    export ARIZE_PHOENIX_API_KEY=<your-key>
    ```
  </Step>

  <Step title="运行 Agent">
    <CodeGroup>
    ```bash Mac
    python cookbook/observability/arize_phoenix_via_openinference.py
    ```

    ```bash Windows
    python cookbook/observability/arize_phoenix_via_openinference.py
    ```
    </CodeGroup>
  </Step>
</Steps>