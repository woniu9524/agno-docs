---
title: Langfuse via OpenInference
---

## 概要

この例では、Agno エージェントを OpenInference でインストルメント化し、トレースを Langfuse に送信する方法を示します。

## コード

```python
import base64
import os

from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.yfinance import YFinanceTools
from openinference.instrumentation.agno import AgnoInstrumentor
from opentelemetry import trace as trace_api
from opentelemetry.exporter.otlp.proto.http.trace_exporter import OTLPSpanExporter
from opentelemetry.sdk.trace import TracerProvider
from opentelemetry.sdk.trace.export import SimpleSpanProcessor

LANGFUSE_AUTH = base64.b64encode(
    f"{os.getenv('LANGFUSE_PUBLIC_KEY')}:{os.getenv('LANGFUSE_SECRET_KEY')}".encode()
).decode()
os.environ["OTEL_EXPORTER_OTLP_ENDPOINT"] = (
    "https://us.cloud.langfuse.com/api/public/otel"  # 🇺🇸 US データリージョン
)
# os.environ["OTEL_EXPORTER_OTLP_ENDPOINT"]="https://cloud.langfuse.com/api/public/otel" # 🇪🇺 EU データリージョン
# os.environ["OTEL_EXPORTER_OTLP_ENDPOINT"]="http://localhost:3000/api/public/otel" # 🏠 ローカルデプロイメント (>= v3.22.0)

os.environ["OTEL_EXPORTER_OTLP_HEADERS"] = f"Authorization=Basic {LANGFUSE_AUTH}"

tracer_provider = TracerProvider()
tracer_provider.add_span_processor(SimpleSpanProcessor(OTLPSpanExporter()))
trace_api.set_tracer_provider(tracer_provider=tracer_provider)

# agno のインストルメント化を開始
AgnoInstrumentor().instrument()

agent = Agent(
    name="Stock Price Agent",
    model=OpenAIChat(id="gpt-4o-mini"),
    tools=[YFinanceTools()],
    instructions="あなたは株価エージェントです。株のアナリストのようなスタイルで質問に答えてください。",
    debug_mode=True,
)

agent.print_response("現在のテスラの株価はいくらですか？")
```

## 使用方法

<Steps>
  <Step title="依存関係のインストール">
    ```bash
    pip install agno openai langfuse opentelemetry-sdk opentelemetry-exporter-otlp openinference-instrumentation-agno
    ```
  </Step>

  <Step title="環境変数の設定">
    ```bash
    export LANGFUSE_PUBLIC_KEY=<your-public-key>
    export LANGFUSE_SECRET_KEY=<your-secret-key>
    ```
  </Step>

  <Step title="エージェントの実行">
    <CodeGroup>
    ```bash Mac
    python cookbook/observability/langfuse_via_openinference.py
    ```

    ```bash Windows
    python cookbook/observability/langfuse_via_openinference.py
    ```
    </CodeGroup>
  </Step>
</Steps>

## 注意事項

- **データリージョン**: 必要に応じて、データリージョンまたはローカルデプロイメントに合わせて `OTEL_EXPORTER_OTLP_ENDPOINT` を調整してください。
  - 米国リージョンの場合は `https://us.cloud.langfuse.com/api/public/otel`
  - EU リージョンの場合は `https://cloud.langfuse.com/api/public/otel`
  - ローカルデプロイメントの場合は `http://localhost:3000/api/public/otel`