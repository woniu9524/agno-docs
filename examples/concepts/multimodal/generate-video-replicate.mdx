## 代码

```python
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.replicate import ReplicateTools

video_agent = Agent(
    name="视频生成代理",
    model=OpenAIChat(id="gpt-4o"),
    tools=[
        ReplicateTools(
            model="tencent/hunyuan-video:847dfa8b01e739637fc76f480ede0c1d76408e1d694b830b5dfb8e547bf98405"
        )
    ],
    description="你是一个可以使用Replicate API生成视频的AI代理。",
    instructions=[
        "当用户要求你创建视频时，使用`generate_media`工具来创建视频。",
        "将URL以原始格式返回给用户。",
        "不要将视频URL转换为Markdown或其他格式。",
    ],
    markdown=True,
    debug_mode=True,
    show_tool_calls=True,
)

video_agent.print_response("生成一个沙漠中马的视频。")
```

## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置你的API密钥">
    ```bash
    export OPENAI_API_KEY=xxx
    export REPLICATE_API_TOKEN=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U openai replicate agno
    ```
  </Step>

  <Step title="运行代理">
    <CodeGroup>
    ```bash Mac
    python cookbook/agent_concepts/multimodal/generate_video_using_replicate.py
    ```

    ```bash Windows
    python cookbook/agent_concepts/multimodal/generate_video_using_replicate.py
    ```
    </CodeGroup>
  </Step>
</Steps>