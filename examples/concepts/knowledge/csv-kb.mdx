---
title: CSVナレッジベース
---

## コード

```python
from pathlib import Path

from agno.agent import Agent
from agno.knowledge.csv import CSVKnowledgeBase
from agno.vectordb.pgvector import PgVector

db_url = "postgresql+psycopg://ai:ai@localhost:5532/ai"

knowledge_base = CSVKnowledgeBase(
    path=Path("data/csvs"),
    vector_db=PgVector(
        table_name="csv_documents",
        db_url=db_url,
    ),
    num_documents=5,  # 検索時に返すドキュメント数
)
# ナレッジベースをロード
knowledge_base.load(recreate=False)

# ナレッジベースでエージェントを初期化
agent = Agent(
    knowledge=knowledge_base,
    search_knowledge=True,
)

# エージェントを使用
agent.print_response("ナレッジベースについて何か質問してください", markdown=True)
```

## 使用方法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="ライブラリのインストール">
    ```bash
    pip install -U sqlalchemy 'psycopg[binary]' pgvector agno
    ```
  </Step>

  <Step title="PgVectorの実行">
    ```bash
    docker run -d \
      -e POSTGRES_DB=ai \
      -e POSTGRES_USER=ai \
      -e POSTGRES_PASSWORD=ai \
      -e PGDATA=/var/lib/postgresql/data/pgdata \
      -v pgvolume:/var/lib/postgresql/data \
      -p 5532:5432 \
      --name pgvector \
      agnohq/pgvector:16
    ```
  </Step>

  <Step title="エージェントの実行">
    <CodeGroup>
    ```bash Mac
    python cookbook/agent_concepts/knowledge/csv_kb.py
    ```

    ```bash Windows
    python cookbook/agent_concepts/knowledge/csv_kb.py
    ```
    </CodeGroup>
  </Step>
</Steps>