---
title: Docx でロード時にフィルタリングする
description: Docx ドキュメントとユーザー固有のメタデータを使用して、ロード時にナレッジベースをフィルタリングする方法を学びます。
---

## コード

```python
from agno.agent import Agent
from agno.knowledge.docx import DocxKnowledgeBase
from agno.utils.media import (
    SampleDataFileExtension,
    download_knowledge_filters_sample_data,
)
from agno.vectordb.lancedb import LanceDb

# すべてのサンプル CV をダウンロードし、そのパスを取得します
downloaded_cv_paths = download_knowledge_filters_sample_data(
    num_files=5, file_extension=SampleDataFileExtension.DOCX
)

# LanceDB を初期化します
# デフォルトでは、データは /tmp/lancedb に保存されます
vector_db = LanceDb(
    table_name="recipes",
    uri="tmp/lancedb",  # データを別の場所に保存するには、このパスを変更できます
)

# ステップ 1: ドキュメントとメタデータでナレッジベースを初期化します
# ------------------------------------------------------------------------------
# ナレッジベースをロードする際に、フィルタリングに使用されるメタデータを添付できます
# このメタデータには、ユーザー ID、ドキュメントの種類、日付、またはその他の属性を含めることができます

# DocxKnowledgeBase を初期化します
knowledge_base = DocxKnowledgeBase(
    vector_db=vector_db,
    num_documents=5,
)

knowledge_base.load_document(
    path=downloaded_cv_paths[0],
    metadata={"user_id": "jordan_mitchell", "document_type": "cv", "year": 2025},
    recreate=True,  # 最初の実行時のみ True に設定し、その後は False に設定してください
)

# ユーザー 2 のメタデータを持つ 2 番目のドキュメントをロードします
knowledge_base.load_document(
    path=downloaded_cv_paths[1],
    metadata={"user_id": "taylor_brooks", "document_type": "cv", "year": 2025},
)

# ユーザー 3 のメタデータを持つ 2 番目のドキュメントをロードします
knowledge_base.load_document(
    path=downloaded_cv_paths[2],
    metadata={"user_id": "morgan_lee", "document_type": "cv", "year": 2025},
)

# ユーザー 4 のメタデータを持つ 2 番目のドキュメントをロードします
knowledge_base.load_document(
    path=downloaded_cv_paths[3],
    metadata={"user_id": "casey_jordan", "document_type": "cv", "year": 2025},
)

# ユーザー 5 のメタデータを持つ 2 番目のドキュメントをロードします
knowledge_base.load_document(
    path=downloaded_cv_paths[4],
    metadata={"user_id": "alex_rivera", "document_type": "cv", "year": 2025},
)

# ステップ 2: 異なるフィルタの組み合わせでナレッジベースをクエリします
# ------------------------------------------------------------------------------
# 実行したい例のコメントを解除してください

# オプション 1: Agent でのフィルタリング
# ナレッジベースで Agent を初期化します
agent = Agent(
    knowledge=knowledge_base,
    search_knowledge=True,
    knowledge_filters={"user_id": "jordan_mitchell"},
)
agent.print_response(
    "Jordan Mitchell の経験とスキルについて教えてください",
    markdown=True,
)

# agent = Agent(
#     knowledge=knowledge_base,
#     search_knowledge=True,
# )
# agent.print_response(
#     "Jordan Mitchell の経験とスキルについて教えてください",
#     knowledge_filters = {"user_id": "jordan_mitchell"},
#     markdown=True,
# )
```

## 使用方法

<Steps>
  <Step title="ライブラリのインストール">
    ```bash
    pip install -U agno openai lancedb
    ```
  </Step>

  <Step title="例の実行">
    <CodeGroup>
    ```bash Mac
    python cookbook/agent_concepts/knowledge/filters/docx/filtering_on_load.py
    ```

    ```bash Windows
    python cookbook/agent_concepts/knowledge/filters/docx/filtering_on_load.py
    ```
    </CodeGroup>
  </Step>
</Steps>