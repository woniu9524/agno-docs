---
title: Weaviateでのフィルタリング
description: Weaviateでユーザー固有のメタデータを持つPDFドキュメントを使用して、ナレッジベース検索をフィルタリングする方法を学びます。
---

## コード

```python
from agno.agent import Agent
from agno.knowledge.pdf import PDFKnowledgeBase
from agno.utils.media import (
    SampleDataFileExtension,
    download_knowledge_filters_sample_data,
)
from agno.vectordb.search import SearchType
from agno.vectordb.weaviate import Distance, VectorIndex, Weaviate

# すべてのサンプルCVをダウンロードし、そのパスを取得します
downloaded_cv_paths = download_knowledge_filters_sample_data(
    num_files=5, file_extension=SampleDataFileExtension.PDF
)

# ステップ1: ドキュメントとメタデータでナレッジベースを初期化します
# ------------------------------------------------------------------------------
# ナレッジベースを初期化する際に、フィルタリングに使用されるメタデータを添付できます
# このメタデータには、ユーザーID、ドキュメントタイプ、日付、またはその他の属性を含めることができます

vector_db = Weaviate(
    collection="recipes",
    vector_index=VectorIndex.HNSW,
    distance=Distance.COSINE,
    local=False,  # Weaviate Cloudを使用する場合はFalseに設定し、ローカルインスタンスを使用する場合はTrueに設定します
)

knowledge_base = PDFKnowledgeBase(
    path=[
        {
            "path": downloaded_cv_paths[0],
            "metadata": {
                "user_id": "jordan_mitchell",
                "document_type": "cv",
                "year": 2025,
            },
        },
        {
            "path": downloaded_cv_paths[1],
            "metadata": {
                "user_id": "taylor_brooks",
                "document_type": "cv",
                "year": 2025,
            },
        },
        {
            "path": downloaded_cv_paths[2],
            "metadata": {
                "user_id": "morgan_lee",
                "document_type": "cv",
                "year": 2025,
            },
        },
        {
            "path": downloaded_cv_paths[3],
            "metadata": {
                "user_id": "casey_jordan",
                "document_type": "cv",
                "year": 2025,
            },
        },
        {
            "path": downloaded_cv_paths[4],
            "metadata": {
                "user_id": "alex_rivera",
                "document_type": "cv",
                "year": 2025,
            },
        },
    ],
    vector_db=vector_db,
)

# すべてのドキュメントをベクトルデータベースにロードします
knowledge_base.load(recreate=True)

# ステップ2: 異なるフィルタの組み合わせでナレッジベースをクエリします
# ------------------------------------------------------------------------------

agent = Agent(
    knowledge=knowledge_base,
    search_knowledge=True,
)

agent.print_response(
    "Jordan Mitchellの経験とスキルについて教えてください",
    knowledge_filters={"user_id": "jordan_mitchell"},
    markdown=True,
)
```

## 使用方法

<Steps>
  <Step title="ライブラリのインストール">
    ```bash
    pip install -U agno weaviate-client openai
    ```
  </Step>

  <Step title="例の実行">
    <CodeGroup>
    ```bash Mac
    python cookbook/agent_concepts/knowledge/filters/filtering_weaviate.py
    ```

    ```bash Windows
    python cookbook/agent_concepts/knowledge/filters/filtering_weaviate.py
    ```
    </CodeGroup>
  </Step>
</Steps>