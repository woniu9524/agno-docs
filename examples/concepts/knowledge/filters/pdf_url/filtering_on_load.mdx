---
title: Pdf-Url でロード時にフィルタリング
description: ユーザー固有のメタデータを持つ Pdf-Url ドキュメントを使用して、ロード時にナレッジベースをフィルタリングする方法を学びます。
---

## コード

```python
from agno.agent import Agent
from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
from agno.vectordb.lancedb import LanceDb

# LanceDB の初期化
# デフォルトでは、データは /tmp/lancedb に保存されます
vector_db = LanceDb(
    table_name="recipes",
    uri="tmp/lancedb",  # データを別の場所に保存するには、このパスを変更できます
)

# ステップ 1: ドキュメントとメタデータでナレッジベースを初期化
# ------------------------------------------------------------------------------
# ナレッジベースをロードする際に、フィルタリングに使用されるメタデータを添付できます。
# このメタデータには、ユーザー ID、ドキュメントの種類、日付、またはその他の属性を含めることができます。

# ナレッジベースの初期化
knowledge_base = PDFUrlKnowledgeBase(
    vector_db=vector_db,
)

knowledge_base.load_document(
    url="https://agno-public.s3.amazonaws.com/recipes/thai_recipes_short.pdf",
    metadata={"cuisine": "Thai", "source": "Thai Cookbook"},
    recreate=False,  # 最初の実行時にのみ使用、True/False
)

knowledge_base.load_document(
    url="https://agno-public.s3.amazonaws.com/recipes/cape_recipes_short_2.pdf",
    metadata={"cuisine": "Cape", "source": "Cape Cookbook"},
)


# オプション 1: エージェントでのフィルタリング
# ナレッジベースでエージェントを初期化
agent = Agent(
    knowledge=knowledge_base,
    search_knowledge=True,
    knowledge_filters={
        "cuisine": "Thai"
    },  # これにより、タイ料理に関連付けられたドキュメントからの情報のみが返されます
)
agent.print_response(
    "Pad Thai の作り方を教えてください",
    markdown=True,
)

# # # オプション 2: 実行時/print_response でのフィルタリング
# agent = Agent(
#     knowledge=knowledge_base,
#     search_knowledge=True,
# )
# agent.print_response(
#     "Cape Malay Curry の作り方を教えてください",
#     knowledge_filters={"cuisine": "Cape"},
#     markdown=True,
# )
```

## 使用方法

<Steps>
  <Step title="ライブラリのインストール">
    ```bash
    pip install -U agno openai lancedb
    ```
  </Step>

  <Step title="例の実行">
    <CodeGroup>
    ```bash Mac
    python cookbook/agent_concepts/knowledge/filters/pdf_url/filtering_on_load.py
    ```

    ```bash Windows
    python cookbook/agent_concepts/knowledge/filters/pdf_url/filtering_on_load.py
    ```
    </CodeGroup>
  </Step>
</Steps>