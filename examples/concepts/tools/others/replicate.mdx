---
title: Replicate ツールの複製
---

## コード

```python cookbook/tools/replicate_tools.py
from agno.agent import Agent
from agno.tools.replicate import ReplicateTools

agent = Agent(
    tools=[ReplicateTools()],
    show_tool_calls=True,
    markdown=True,
)
agent.print_response("Generate an image of a cyberpunk city")
```

## 使用方法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="API トークンを設定する">
    ```bash
    export REPLICATE_API_TOKEN=xxx
    export OPENAI_API_KEY=xxx
    ```
  </Step>

  <Step title="ライブラリをインストールする">
    ```bash
    pip install -U replicate openai agno
    ```
  </Step>

  <Step title="エージェントを実行する">
    <CodeGroup>
    ```bash Mac
    python cookbook/tools/replicate_tools.py
    ```

    ```bash Windows
    python cookbook/tools/replicate_tools.py
    ```
    </CodeGroup>
  </Step>
</Steps>