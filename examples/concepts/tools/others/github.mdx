## 代码

```python cookbook/tools/github_tools.py
from agno.agent import Agent
from agno.tools.github import GithubTools

agent = Agent(
    instructions=[
        "使用你的工具来回答关于仓库 agno-agi/agno 的问题",
        "除非明确要求，否则不要创建任何 issue 或 pull request",
    ],
    tools=[GithubTools()],
    show_tool_calls=True,
)
agent.print_response("列出打开的 pull requests", markdown=True)
```

## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置你的 GitHub token">
    ```bash
    export GITHUB_TOKEN=xxx
    export OPENAI_API_KEY=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U PyGithub openai agno
    ```
  </Step>

  <Step title="运行 Agent">
    <CodeGroup>
    ```bash Mac
    python cookbook/tools/github_tools.py
    ```

    ```bash Windows
    python cookbook/tools/github_tools.py
    ```
    </CodeGroup>
  </Step>
</Steps>