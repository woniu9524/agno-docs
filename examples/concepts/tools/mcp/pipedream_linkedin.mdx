---
title: "Pipedream LinkedIn"
description: "この例では、LinkedIn Pipedream MCPサーバーをAgno Agentsで使用する方法を示します。"
---

## コード

```python
"""
💻 Pipedream LinkedIn MCP

この例では、Pipedream MCPサーバー（この場合はLinkedInのもの）をAgno Agentsで使用する方法を示します。

1. PipedreamとLinkedInアカウントを接続します: https://mcp.pipedream.com/app/linkedin
2. Pipedream MCPサーバーのURLを取得します: https://mcp.pipedream.com/app/linkedin
3. 上記で取得したMCPサーバーのURLをMCP_SERVER_URL環境変数に設定します
4. 依存関係をインストールします: pip install agno mcp-sdk
"""

import asyncio
import os

from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.mcp import MCPTools
from agno.utils.log import log_exception

mcp_server_url = os.getenv("MCP_SERVER_URL")


async def run_agent(task: str) -> None:
    try:
        async with MCPTools(
            url=mcp_server_url, transport="sse", timeout_seconds=20
        ) as mcp:
            agent = Agent(
                model=OpenAIChat(id="gpt-4o-mini"),
                tools=[mcp],
                markdown=True,
            )
            await agent.aprint_response(
                message=task,
                stream=True,
            )
    except Exception as e:
        log_exception(f"予期せぬエラー: {e}")


if __name__ == "__main__":
    asyncio.run(
        run_agent("LinkedInでPipedreamの組織をチェックして、それについて教えてください")
    )