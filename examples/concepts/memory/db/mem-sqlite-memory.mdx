---
title: SQLite メモリ ストレージ
---

## コード

```python cookbook/agent_concepts/memory/sqlite_memory.py
"""
この例では、Memory クラスと SQLite ストレージの使用方法を示します。
"""

from agno.agent.agent import Agent
from agno.memory.v2.db.sqlite import SqliteMemoryDb
from agno.memory.v2.memory import Memory
from agno.models.openai import OpenAIChat
from agno.storage.sqlite import SqliteStorage

# SQLite メモリデータベースの作成
memory_db = SqliteMemoryDb(
    table_name="agent_memories",  # データベースで使用するテーブル名
    db_file="tmp/memory.db",      # SQLite データベースファイルへのパス
)

# SQLite バックエンドを持つメモリインスタンスの作成
memory = Memory(db=memory_db)

# テーブルが存在しない場合は作成されます
memory.clear()

# セッションおよびユーザー識別子
session_id = "sqlite_memories"
user_id = "sqlite_user"

# メモリと SQLite ストレージを持つエージェントの作成
agent = Agent(
    model=OpenAIChat(id="gpt-4o-mini"),
    memory=memory,
    storage=SqliteStorage(
        table_name="agent_sessions",
        db_file="tmp/memory.db"
    ),
    enable_user_memories=True,
    enable_session_summaries=True,
)

# 最初の対話 - 個人情報の紹介
agent.print_response(
    "私の名前はジョン・ドウです。週末は山でハイキングをするのが好きです。",
    stream=True,
    user_id=user_id,
    session_id=session_id,
)

# 2 回目の対話 - メモリが保存されたかどうかのテスト
agent.print_response(
    "私の趣味は何ですか？",
    stream=True,
    user_id=user_id,
    session_id=session_id
)

# SQLite に保存されたメモリの表示
memories = memory.get_user_memories(user_id=user_id)
print("SQLite に保存されたメモリ:")
for i, m in enumerate(memories):
    print(f"{i}: {m.memory}")
```

## 使用方法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="環境変数の設定">
    ```bash
    export OPENAI_API_KEY=xxx
    ```
  </Step>

  <Step title="ライブラリのインストール">
    ```bash
    pip install -U agno openai
    ```
  </Step>

  <Step title="例の実行">
    <CodeGroup>
    ```bash Mac/Linux
    python cookbook/agent_concepts/memory/sqlite_memory.py
    ```

    ```bash Windows
    python cookbook/agent_concepts/memory/sqlite_memory.py
    ```
    </CodeGroup>
  </Step>
</Steps>