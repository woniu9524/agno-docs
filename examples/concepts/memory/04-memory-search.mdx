---
title: Memory Search
---

この例では、さまざまな検索方法を使用してユーザーの記憶を検索する方法を示します。

- last_n: 最後のn件の記憶を取得します
- first_n: 最初のn件の記憶を取得します
- semantic: セマンティック検索を使用して記憶を取得します

## コード

```python cookbook/agent_concepts/memory/05_memory_search.py
from agno.memory.v2 import Memory, UserMemory
from agno.memory.v2.db.sqlite import SqliteMemoryDb
from agno.models.google.gemini import Gemini
from rich.pretty import pprint

memory_db = SqliteMemoryDb(table_name="memory", db_file="tmp/memory.db")
# この例のためにリセットします
memory_db.clear()

memory = Memory(model=Gemini(id="gemini-2.0-flash-exp"), db=memory_db)

john_doe_id = "john_doe@example.com"
memory.add_user_memory(
    memory=UserMemory(memory="ユーザーは週末に山でのハイキングを楽しんでいます"),
    user_id=john_doe_id,
)
memory.add_user_memory(
    memory=UserMemory(
        memory="ユーザーは寝る前にSF小説を読むのを楽しんでいます"
    ),
    user_id=john_doe_id,
)
print("John Doe's memories:")
pprint(memory.memories)

memories = memory.search_user_memories(
    user_id=john_doe_id, limit=1, retrieval_method="last_n"
)
print("\nJohn Doe's last_n memories:")
pprint(memories)

memories = memory.search_user_memories(
    user_id=john_doe_id, limit=1, retrieval_method="first_n"
)
print("\nJohn Doe's first_n memories:")
pprint(memories)

memories = memory.search_user_memories(
    user_id=john_doe_id,
    query="ユーザーは週末に何をするのが好きですか？",
    retrieval_method="agentic",
)
print("\nJohn Doe's memories similar to the query (agentic):")
pprint(memories)
```

## 使用方法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="ライブラリのインストール">
    ```bash
    pip install -U agno
    ```
  </Step>

  <Step title="例の実行">
    <CodeGroup>
    ```bash Mac
    python cookbook/agent_concepts/memory/05_memory_search.py
    ```

    ```bash Windows
    python cookbook/agent_concepts/memory/05_memory_search.py
    ```
    </CodeGroup>
  </Step>
</Steps>