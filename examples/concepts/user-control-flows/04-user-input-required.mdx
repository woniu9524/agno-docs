---
title: ユーザー入力が必要
description: この例では、エージェント実行中にユーザー入力の収集を実装する方法を示します。これにより、ユーザーはツールパラメータに特定の情報を提供できます。
---

## コード

```python cookbook/agent_concepts/user_control_flows/user_input_required.py
from typing import List

from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools import tool
from agno.tools.function import UserInputField
from agno.utils import pprint

# ユーザーがすべてのフィールドを提供するように、user_input_fields を指定するか、空のままにすることができます
@tool(requires_user_input=True, user_input_fields=["to_address"])
def send_email(subject: str, body: str, to_address: str) -> str:
    """
    メールを送信します。

    Args:
        subject (str): メールの件名。
        body (str): メールの本文。
        to_address (str): 送信先のメールアドレス。
    """
    return f"Sent email to {to_address} with subject {subject} and body {body}"

agent = Agent(
    model=OpenAIChat(id="gpt-4o-mini"),
    tools=[send_email],
    markdown=True,
)

agent.run("Send an email with the subject 'Hello' and the body 'Hello, world!'")
if agent.is_paused:
    for tool in agent.run_response.tools_requiring_user_input:
        input_schema: List[UserInputField] = tool.user_input_schema

        for field in input_schema:
            # フィールド情報をユーザーに表示します
            print(f"\nフィールド: {field.name}")
            print(f"説明: {field.description}")
            print(f"タイプ: {field.field_type}")

            # ユーザー入力を取得します
            if field.value is None:
                user_value = input(f"{field.name} の値を入力してください: ")
                # フィールド値を更新します
                field.value = user_value
            else:
                print(f"値: {field.value}")

    run_response = agent.continue_run()
    pprint.pprint_run_response(run_response)
```

## 使用方法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="API キーを設定する">
    ```bash
    export OPENAI_API_KEY=xxx
    ```
  </Step>

  <Step title="ライブラリをインストールする">
    ```bash
    pip install -U agno openai
    ```
  </Step>

  <Step title="例を実行する">
    <CodeGroup>
    ```bash Mac
    python cookbook/agent_concepts/user_control_flows/user_input_required.py
    ```

    ```bash Windows
    python cookbook/agent_concepts/user_control_flows/user_input_required.py
    ```
    </CodeGroup>
  </Step>
</Steps>

## 主な機能

- ユーザー入力を必要とするツールをマークするために `@tool(requires_user_input=True)` を使用します
- `user_input_fields` を使用して、どのフィールドにユーザー入力が必要かを指定できます
- ユーザー入力収集のための動的なフォームライクインターフェースを実装します
- ユーザー提供の値とエージェント提供の値の両方を処理します

## ユースケース

- 操作に必要なパラメータを収集する