---
title: 動的なユーザー入力 (エージェントによる制御)
description: この例では、`UserControlFlowTools` を使用して動的なユーザー入力収集を実装する方法を示し、エージェントが実行中に必要に応じて情報を要求できるようにします。
---

## コード

```python cookbook/agent_concepts/user_control_flows/agentic_user_input.py
from typing import List

from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.function import UserInputField
from agno.tools.toolkit import Toolkit
from agno.tools.user_control_flow import UserControlFlowTools
from agno.utils import pprint

class EmailTools(Toolkit):
    def __init__(self, *args, **kwargs):
        super().__init__(
            name="EmailTools", tools=[self.send_email, self.get_emails], *args, **kwargs
        )

    def send_email(self, subject: str, body: str, to_address: str) -> str:
        """指定された件名と本文で、指定されたアドレスにメールを送信します。

        Args:
            subject (str): メールの件名。
            body (str): メールの本文。
            to_address (str): 送信先のアドレス。
        """
        return f"Sent email to {to_address} with subject {subject} and body {body}"

    def get_emails(self, date_from: str, date_to: str) -> str:
        """指定された日付間のすべてのメールを取得します。

        Args:
            date_from (str): 開始日 (YYYY-MM-DD 形式)。
            date_to (str): 終了日 (YYYY-MM-DD 形式)。
        """
        return [
            {
                "subject": "Hello",
                "body": "Hello, world!",
                "to_address": "test@test.com",
                "date": date_from,
            },
            {
                "subject": "Random other email",
                "body": "This is a random other email",
                "to_address": "john@doe.com",
                "date": date_to,
            },
        ]

agent = Agent(
    model=OpenAIChat(id="gpt-4o-mini"),
    tools=[EmailTools(), UserControlFlowTools()],
    markdown=True,
    debug_mode=True,
)

run_response = agent.run("Send an email with the body 'What is the weather in Tokyo?'")
# エージェントがユーザー入力に満足するまで実行を継続するために while ループを使用します
while run_response.is_paused:
    for tool in run_response.tools_requiring_user_input:
        input_schema: List[UserInputField] = tool.user_input_schema

        for field in input_schema:
            # フィールド情報をユーザーに表示します
            print(f"\nField: {field.name}")
            print(f"Description: {field.description}")
            print(f"Type: {field.field_type}")

            # ユーザー入力を取得します
            if field.value is None:
                user_value = input(f"Please enter a value for {field.name}: ")
            else:
                print(f"Value: {field.value}")
                user_value = field.value

            # フィールド値を更新します
            field.value = user_value

    run_response = agent.continue_run(run_response=run_response)
    if not run_response.is_paused:
        pprint.pprint_run_response(run_response)
        break
```

## 使用方法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="API キーを設定する">
    ```bash
    export OPENAI_API_KEY=xxx
    ```
  </Step>

  <Step title="ライブラリをインストールする">
    ```bash
    pip install -U agno openai
    ```
  </Step>

  <Step title="例を実行する">
    <CodeGroup>
    ```bash Mac
    python cookbook/agent_concepts/user_control_flows/agentic_user_input.py
    ```

    ```bash Windows
    python cookbook/agent_concepts/user_control_flows/agentic_user_input.py
    ```
    </CodeGroup>
  </Step>
</Steps>

## 主な機能

- 動的なユーザー入力収集のために `UserControlFlowTools` を使用します
- ユーザー入力が必要になる可能性のある複数のツールを持つツールキットを実装します
- 複数回のユーザー入力収集を処理します
- 各入力ラウンド後にエージェントの実行を継続する方法を示します

## ユースケース

- 動的なフォームのようなインタラクション