---
title: 流式代理
---

## 代码

```python cookbook/models/aws/bedrock/basic_stream.py
from typing import Iterator  # noqa
from agno.agent import Agent, RunResponse  # noqa
from agno.models.aws import AwsBedrock

agent = Agent(
    model=AwsBedrock(id="mistral.mistral-large-2402-v1:0"), markdown=True
)

# 在变量中获取响应
# run_response: Iterator[RunResponse] = agent.run("写一个2句的恐怖故事", stream=True)
# for chunk in run_response:
#     print(chunk.content)

# 在终端打印响应
agent.print_response("写一个2句的恐怖故事", stream=True)
```

## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置您的 AWS 凭证">
    ```bash
    export AWS_ACCESS_KEY_ID=***
    export AWS_SECRET_ACCESS_KEY=***
    export AWS_REGION=***
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U boto3 agno
    ```
  </Step>

  <Step title="运行代理">
    <CodeGroup>
    ```bash Mac
    python cookbook/models/aws/bedrock/basic_stream.py
    ```

    ```bash Windows
    python cookbook/models/aws/bedrock/basic_stream.py
    ```
    </CodeGroup>
  </Step>
</Steps>