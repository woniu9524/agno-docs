---
title: 基础 Agent
---

## 代码

```python cookbook/models/ibm/watsonx/basic.py
from agno.agent import Agent, RunResponse
from agno.models.ibm import WatsonX

agent = Agent(model=WatsonX(id="ibm/granite-20b-code-instruct"), markdown=True)

# 将响应存储在变量中
# run: RunResponse = agent.run("Share a 2 sentence horror story")
# print(run.content)

# 在终端中打印响应
agent.print_response("Share a 2 sentence horror story")
```

## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置您的 API 密钥">
    ```bash
    export IBM_WATSONX_API_KEY=xxx
    export IBM_WATSONX_PROJECT_ID=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U ibm-watsonx-ai agno
    ```
  </Step>

  <Step title="运行 Agent">
    <CodeGroup>
    ```bash Mac
    python cookbook/models/ibm/watsonx/basic.py
    ```

    ```bash Windows
    python cookbook\models\ibm\watsonx\basic.py
    ```
    </CodeGroup>
  </Step>
</Steps>

此示例使用 IBM WatsonX 模型创建了一个 Agent，并将响应直接打印到终端。`markdown=True` 参数告诉 Agent 将输出格式化为 markdown，这对于显示富文本内容非常有用。