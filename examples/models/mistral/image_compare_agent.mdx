---
title: 画像比較エージェント
description: Mistral のビジョンモデルを使用して 2 つの画像を比較し、違いを特定する例
---

## 概要
この例では、Mistral のビジョンモデルを使用して 2 つの異なる画像を分析および比較する方法を示します。
エージェントは画像間の違いを特定して説明するため、視覚的な比較タスクに役立ちます。

## コード

```python cookbook/models/mistral/image_compare_agent.py
from agno.agent import Agent
from agno.media import Image
from agno.models.mistral.mistral import MistralChat

agent = Agent(
    model=MistralChat(id="pixtral-12b-2409"),
    markdown=True,
)

# エッフェル塔の 2 つの異なる画像を比較する
agent.print_response(
    "2 つの画像の違いは何ですか？",
    images=[
        Image(url="https://tripfixers.com/wp-content/uploads/2019/11/eiffel-tower-with-snow.jpeg"),
        Image(url="https://assets.visitorscoverage.com/production/wp-content/uploads/2024/04/AdobeStock_626542468-min-1024x683.jpeg"),
    ],
    stream=True,
)
```

## 使用方法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="API キーの設定">
    ```bash
    export MISTRAL_API_KEY=xxx
    ```
  </Step>

  <Step title="ライブラリのインストール">
    ```bash
    pip install -U mistralai agno
    ```
  </Step>

  <Step title="エージェントの実行">
    <CodeGroup>
    ```bash Mac
    python cookbook/models/mistral/image_compare_agent.py
    ```

    ```bash Windows
    python cookbook/models/mistral/image_compare_agent.py
    ```
    </CodeGroup>
  </Step>
</Steps>