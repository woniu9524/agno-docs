---
title: 基础代理
---

## 代码

```python cookbook/models/openai/responses/basic.py
from agno.agent import Agent, RunResponse  # noqa
from agno.models.openai import OpenAIResponses

agent = Agent(model=OpenAIResponses(id="gpt-4o"), markdown=True)

# 在变量中获取响应
# run: RunResponse = agent.run("分享一个两句的恐怖故事")
# print(run.content)

# 在终端打印响应
agent.print_response("分享一个两句的恐怖故事")

agent.run_response.metrics
```

## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置您的 API 密钥">
    ```bash
    export OPENAI_API_KEY=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U openai agno
    ```
  </Step>

  <Step title="运行代理">
    <CodeGroup>
    ```bash Mac
    python cookbook/models/openai/responses/basic.py
    ```

    ```bash Windows
    python cookbook/models/openai/responses/basic.py
    ```
    </CodeGroup>
  </Step>
</Steps>