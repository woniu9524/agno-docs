---
title: Agent レスポンスのパフォーマンス
description: レスポンスが与えられた Agent の実行時間とメモリ使用量を分析する方法を示す例。
---

## コード

```python
"""依存関係をインストールするには `pip install openai agno memory_profiler` を実行してください。"""

from agno.agent import Agent
from agno.eval.performance import PerformanceEval
from agno.models.openai import OpenAIChat


def simple_response():
    agent = Agent(
        model=OpenAIChat(id="gpt-4o-mini"),
        system_message="簡潔に、一文で返信してください。",
    )
    response = agent.run("フランスの首都は？")
    return response


simple_response_perf = PerformanceEval(
    func=simple_response, num_iterations=1, warmup_runs=0
)

if __name__ == "__main__":
    simple_response_perf.run(print_results=True, print_summary=True)