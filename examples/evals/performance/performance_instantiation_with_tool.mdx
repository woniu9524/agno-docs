---
title: Tool を使用したエージェントのインスタンス化におけるパフォーマンス
description: ツールを使用するエージェントの実行時間とメモリ使用量を分析する方法を示す例。
---

## コード

```python
"""依存関係をインストールするには `pip install agno openai memory_profiler` を実行してください。"""

from typing import Literal

from agno.agent import Agent
from agno.eval.performance import PerformanceEval
from agno.models.openai import OpenAIChat


def get_weather(city: Literal["nyc", "sf"]):
    """天気情報を取得するために使用します。"""
    if city == "nyc":
        return "It might be cloudy in nyc"
    elif city == "sf":
        return "It's always sunny in sf"
    else:
        raise AssertionError("Unknown city")


tools = [get_weather]


def instantiate_agent():
    return Agent(model=OpenAIChat(id="gpt-4o"), tools=tools)


instantiation_perf = PerformanceEval(func=instantiate_agent, num_iterations=1000)

if __name__ == "__main__":
    instantiation_perf.run(print_results=True, print_summary=True)