---
title: 思维工具
sidebarTitle: 思维工具
---

`ThinkingTools` 工具包为代理在执行过程中提供了一个专门的反思空间。该工具包使代理能够使用记事本来思考问题、列出规则、检查信息、验证合规性以及在采取行动之前评估结果。

与让代理立即响应或采取行动的方法不同，该工具包通过为代理提供“思考”其行动、检查自身响应并记录整个对话过程中思考过程的空间，来鼓励周密的考虑。

该工具包包含以下工具：

- `think`：此工具充当代理的记事本，用于推理问题、列出适用规则、验证收集到的信息以及评估计划采取的行动的合规性和正确性。

## 示例

以下是如何使用 `ThinkingTools` 工具包的示例：

```python
from agno.agent import Agent
from agno.models.anthropic import Claude
from agno.tools.thinking import ThinkingTools
from agno.tools.yfinance import YFinanceTools

thinking_agent = Agent(
    model=Claude(id="claude-3-7-sonnet-latest"),
    tools=[
        ThinkingTools(add_instructions=True),
        YFinanceTools(
            stock_price=True,
            analyst_recommendations=True,
            company_info=True,
            company_news=True,
        ),
    ],
    instructions="Use tables where possible",
    show_tool_calls=True,
    markdown=True,
)

thinking_agent.print_response("Write a report comparing NVDA to TSLA", stream=True)
```

该工具包附带默认说明，以帮助代理有效使用该工具。以下是如何启用它们的方法：

```python
thinking_agent = Agent(
    model=Claude(id="claude-3-7-sonnet-latest"),
    tools=[
        ThinkingTools(
            think=True,
            add_instructions=True,
        ),
    ],
)
```

`ThinkingTools` 可与支持函数调用的任何模型提供商一起使用。以下是使用 `OpenAIChat` 的思考代理的示例：

```python
from textwrap import dedent

from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.thinking import ThinkingTools

thinking_agent = Agent(
    model=OpenAIChat(id="gpt-4o"),
    tools=[ThinkingTools(add_instructions=True)],
    instructions=dedent("""\
        You are an expert problem-solving assistant with strong analytical skills! 🧠

        Your approach to problems:
        1. First, break down complex questions into component parts
        2. Clearly state your assumptions
        3. Develop a structured reasoning path
        4. Consider multiple perspectives
        5. Evaluate evidence and counter-arguments
        6. Draw well-justified conclusions

        When solving problems:
        - Use explicit step-by-step reasoning
        - Identify key variables and constraints
        - Explore alternative scenarios
        - Highlight areas of uncertainty
        - Explain your thought process clearly
        \
    """),
    add_datetime_to_instructions=True,
    stream_intermediate_steps=True,
    show_tool_calls=True,
    markdown=True,
)
```

此代理可用于处理需要仔细考虑的复杂问题：

```python
thinking_agent.print_response(
    "We need to implement a new content moderation policy for our platform. "
    stream=True
)
```

或者，

```python
thinking_agent.print_response(
    "Our company is developing a new AI product. We need to consider ethical implications "
    stream=True,
)