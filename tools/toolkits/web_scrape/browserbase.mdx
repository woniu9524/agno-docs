---
title: Browserbase
---

**BrowserbaseTools** は、ヘッドレスブラウザサービスであるBrowserbaseを使用して、エージェントがブラウザ操作を自動化できるようにします。

## 前提条件

以下の例では、[こちら](https://www.browserbase.com/)からサインアップした後のBrowserbase API認証情報と、Playwrightライブラリが必要です。

```shell
pip install browserbase playwright
export BROWSERBASE_API_KEY=xxx
export BROWSERBASE_PROJECT_ID=xxx
```


## 例

以下のエージェントは、Browserbaseを使用して `https://quotes.toscrape.com` にアクセスし、コンテンツを抽出します。その後、ウェブサイトの2ページ目に移動し、そこからも引用を取得します。

```python cookbook/tools/browserbase_tools.py
from agno.agent import Agent
from agno.tools.browserbase import BrowserbaseTools

agent = Agent(
    name="Web Automation Assistant",
    tools=[BrowserbaseTools()],
    instructions=[
        "You are a web automation assistant that can help with:",
        "1. Capturing screenshots of websites",
        "2. Extracting content from web pages",
        "3. Monitoring website changes",
        "4. Taking visual snapshots of responsive layouts",
        "5. Automated web testing and verification",
    ],
    markdown=True,
)

agent.print_response("""
    Visit https://quotes.toscrape.com and:
    1. Extract the first 5 quotes and their authors
    2. Navigate to page 2
    3. Extract the first 5 quotes from page 2
""")
```



## Toolkitパラメータ

| パラメータ    | タイプ   | デフォルト | 説明                                                                                                                                                                                           |
| ------------ | ------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `api_key`    | `str`  | `None`  | Browserbase APIキー。提供されない場合は、BROWSERBASE_API_KEY環境変数が使用されます。                                                                                                              |
| `project_id` | `str`  | `None`  | BrowserbaseプロジェクトID。提供されない場合は、BROWSERBASE_PROJECT_ID環境変数が使用されます。                                                                                                    |
| `base_url`   | `str`  | `None`  | カスタムBrowserbase APIエンドポイントURL。これは、セルフホストされたBrowserbaseインスタンスを使用している場合、または別のリージョンに接続する必要がある場合にのみ使用してください。提供されない場合は、BROWSERBASE_BASE_URL環境変数が使用されます。 |

## Toolkit関数

| 関数           | 説明                                                                                                                             |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `navigate_to`      | URLに移動します。URLとオプションのconnect_urlパラメータを受け取ります。                                                              |
| `screenshot`       | 現在のページのスクリーンショットを撮ります。スクリーンショットを保存するパス、フルページキャプチャ用のブール値、およびオプションのconnect_urlパラメータを受け取ります。 |
| `get_page_content` | 現在のページのHTMLコンテンツを取得します。オプションのconnect_urlパラメータを受け取ります。                                                              |
| `close_session`    | ブラウザセッションを閉じます。                                                                                                         |

## 開発者向けリソース

- [Tools](https://github.com/agno-agi/agno/blob/main/libs/agno/agno/tools/browserbase.py) を表示
- [Cookbook](https://github.com/agno-agi/agno/blob/main/cookbook/tools/browserbase_tools.py) を表示