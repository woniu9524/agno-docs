---
title: Email
---

**EmailTools** を使用すると、エージェントはユーザーにメールを送信できます。エージェントは、特定件名と本文でユーザーにメールを送信できます。

## 例

```python cookbook/tools/email_tools.py
from agno.agent import Agent
from agno.tools.email import EmailTools

receiver_email = "<receiver_email>"
sender_email = "<sender_email>"
sender_name = "<sender_name>"
sender_passkey = "<sender_passkey>"

agent = Agent(
    tools=[
        EmailTools(
            receiver_email=receiver_email,
            sender_email=sender_email,
            sender_name=sender_name,
            sender_passkey=sender_passkey,
        )
    ]
)

agent.print_response("send an email to <receiver_email>")
```

## Toolkit パラメータ

| パラメータ        | タイプ  | デフォルト | 説明                         |
| ----------------- | ----- | ------- | ---------------------------- |
| `receiver_email`  | `str` | -       | 受信者のメールアドレス。     |
| `sender_name`     | `str` | -       | 送信者の名前。               |
| `sender_email`    | `str` | -       | 送信者のメールアドレス。     |
| `sender_passkey`  | `str` | -       | 送信者のメールのパスキー。   |

## Toolkit 関数

| 関数         | 説明                                                                   |
| ------------ | ---------------------------------------------------------------------- |
| `email_user` | 指定された件名と本文でユーザーにメールを送信します。現在はGmailに対応しています。 |

## 開発者向けリソース

- [Tools](https://github.com/agno-agi/agno/blob/main/libs/agno/agno/tools/email.py) を表示
- [Cookbook](https://github.com/agno-agi/agno/blob/main/cookbook/tools/email_tools.py) を表示