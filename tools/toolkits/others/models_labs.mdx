---
title: ModelsLabs
---

## 先决条件

您需要安装 `requests` 库。

```bash
pip install requests
```

设置 `MODELS_LAB_API_KEY` 环境变量。

```bash
export MODELS_LAB_API_KEY=****
```

## 示例

下面的代理将使用 ModelsLabs 根据文本提示生成视频。

```python cookbook/tools/models_labs_tools.py
from agno.agent import Agent
from agno.tools.models_labs import ModelsLabsTools

# 使用 ModelsLabsTools 创建一个 Agent
agent = Agent(tools=[ModelsLabsTools()], name="ModelsLabs Agent")

agent.print_response("Generate a video of a beautiful sunset over the ocean", markdown=True)
```

## 工具包参数

| 参数             | 类型   | 默认值                                           | 描述                                                                 |
| ---------------- | ------ | ------------------------------------------------ | -------------------------------------------------------------------- |
| `api_key`        | `str`  | `None`                                           | 用于身份验证的 ModelsLab API 密钥                                      |
| `wait_for_completion` | `bool` | `False`                                          | 是否等待视频准备就绪                                                   |
| `add_to_eta`     | `int`  | `15`                                             | 添加到预计到达时间 (ETA) 的时间，以考虑获取视频所需的时间                  |
| `max_wait_time`  | `int`  | `60`                                             | 等待视频准备就绪的最大时间                                               |
| `file_type`      | `str`  | `"mp4"`                                          | 要生成的文件的类型                                                     |

## 工具包函数

| 函数           | 描述                                     |
| -------------- | ---------------------------------------- |
| `generate_media` | 根据文本提示生成视频或 GIF |

## 开发者资源

- 查看 [Tools](https://github.com/agno-agi/agno/blob/main/libs/agno/agno/tools/models_labs.py)
- 查看 [Cookbook](https://github.com/agno-agi/agno/blob/main/cookbook/tools/models_labs_tools.py)