---
title: Brave Search
---

**BraveSearch** 使 Agent 能够使用 Brave 搜索引擎在网上查找信息。

## 先决条件

以下示例需要 `brave-search` 库。

```shell
pip install -U brave-search
```
```shell
export BRAVE_API_KEY=***
```

## 示例


```python cookbook/tools/bravesearch_tools.py
from agno.agent import Agent
from agno.tools.bravesearch import BraveSearchTools

agent = Agent(
    tools=[BraveSearchTools()],
    description="You are a news agent that helps users find the latest news.",
    instructions=[
        "Given a topic by the user, respond with 4 latest news items about that topic."
    ],
    show_tool_calls=True,
)
agent.print_response("AI Agents", markdown=True)

```

## Toolkit 参数

| 参数           | 类型  | 默认值 | 描述                                         |
| --------------- | ----- | ------- | --------------------------------------------------- |
| `fixed_max_results` | `int` | `None`  | 可选的固定最大返回结果数。 |
| `fixed_language`    | `str` | `None`  | 可选的固定请求语言。           |


## Toolkit 函数

| 函数        | 描述                                                                                                                                                                                                                                                                            |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `brave_search` | 使用 Brave 搜索指定查询。参数包括搜索词 `query`、最大结果数 `max_results`（默认为 5）、搜索结果的地理区域 `country`（默认为 "US"）以及搜索结果的语言 `language`（默认为 "en"）。返回 JSON 格式的搜索结果字符串。 |

## 开发者资源

- 查看 [Tools](https://github.com/agno-agi/agno/blob/main/libs/agno/agno/tools/bravesearch.py)
- 查看 [Cookbook](https://github.com/agno-agi/agno/blob/main/cookbook/tools/bravesearch_tools.py)