---
title: OpenAI
sidebarTitle: OpenAI
---

OpenAITools を使用すると、Agent はオーディオの文字起こし、画像の生成、テキスト読み上げを実行するために OpenAI モデルと対話できます。

## 前提条件

`OpenAITools` を使用する前に、`openai` ライブラリがインストールされており、OpenAI API キーが設定されていることを確認してください。

1. **ライブラリのインストール:**
   ```bash
   pip install -U openai
   ```

2. **API キーの設定:** [OpenAI](https://platform.openai.com/account/api-keys) から API キーを取得し、環境変数として設定します。

   <CodeGroup>
   ```bash Mac
   export OPENAI_API_KEY="your-openai-api-key"
   ```
   ```bash Windows
   setx OPENAI_API_KEY "your-openai-api-key"
   ```
   </CodeGroup>

## 初期化

`OpenAITools` をインポートし、Agent のツールリストに追加します。

```python
from agno.agent import Agent
from agno.tools.openai import OpenAITools

agent = Agent(
    name="OpenAI Agent",
    tools=[OpenAITools()],
    show_tool_calls=True,
    markdown=True,
)
```

## 使用例

### 1. オーディオの文字起こし

この例では、オーディオファイルを文字起こしする Agent を示します。

```python transcription_agent.py
from pathlib import Path
from agno.agent import Agent
from agno.tools.openai import OpenAITools
from agno.utils.media import download_file

audio_url = "https://agno-public.s3.amazonaws.com/demo_data/sample_conversation.wav"

local_audio_path = Path("tmp/sample_conversation.wav")
download_file(audio_url, local_audio_path)

agent = Agent(
    name="OpenAI Transcription Agent",
    tools=[OpenAITools(transcription_model="whisper-1")],
    show_tool_calls=True,
    markdown=True,
)

agent.print_response(f"Transcribe the audio file located at '{local_audio_path}'")
```

### 2. 画像の生成

この例では、テキストプロンプトに基づいて画像を生成する Agent を示します。

```python image_generation_agent.py
from agno.agent import Agent
from agno.tools.openai import OpenAITools
from agno.utils.media import save_base64_data

agent = Agent(
    name="OpenAI Image Generation Agent",
    tools=[OpenAITools(image_model="dall-e-3")],
    show_tool_calls=True,
    markdown=True,
)

response = agent.run("Generate a photorealistic image of a cozy coffee shop interior")

if response.images:
    save_base64_data(response.images[0].content, "tmp/coffee_shop.png")
```

### 3. 音声の生成

この例では、テキストから音声を生成する Agent を示します。

```python speech_synthesis_agent.py
from agno.agent import Agent
from agno.tools.openai import OpenAITools
from agno.utils.media import save_base64_data

agent = Agent(
    name="OpenAI Speech Agent",
    tools=[OpenAITools(
        text_to_speech_model="tts-1",
        text_to_speech_voice="alloy",
        text_to_speech_format="mp3"
    )],
    show_tool_calls=True,
    markdown=True,
)

agent.print_response("Generate audio for the text: 'Hello, this is a synthesized voice example.'")

response = agent.run_response
if response and response.audio:
    save_base64_data(response.audio[0].base64_audio, "tmp/hello.mp3")
```

<Note> 詳細な例は[こちら](/examples/concepts/tools/models/openai)でご覧ください。 </Note>

## カスタマイズ

文字起こし、画像生成、TTS に使用される基盤となる OpenAI モデルをカスタマイズできます。

```python
OpenAITools(
    transcription_model="whisper-1",
    image_model="dall-e-3",
    text_to_speech_model="tts-1-hd",
    text_to_speech_voice="nova",
    text_to_speech_format="wav"
)
```

## ツールキット関数

`OpenAITools` ツールキットは、次の関数を提供します。

| 関数           | 説明                                                                 |
| ------------------ | --------------------------------------------------------------------------- |
| `transcribe_audio` | ローカルファイルパスまたは公開 URL からオーディオを文字起こしします |
| `generate_image`   | テキストプロンプトに基づいて画像を生成します |
| `generate_speech`  | テキストから音声を合成します |

## 開発者向けリソース

- [ツール](https://github.com/agno-agi/agno/blob/main/libs/agno/agno/tools/openai.py)を表示
- [OpenAI 文字起こしガイド](https://platform.openai.com/docs/guides/speech-to-text)を表示
- [OpenAI 画像生成ガイド](https://platform.openai.com/docs/guides/image-generation?image-generation-model=gpt-image-1)を表示
- [OpenAI テキスト読み上げガイド](https://platform.openai.com/docs/guides/text-to-speech)を表示